# st-theater 项目交接

> 最后核对：2026-08-01（Asia/Shanghai）
> 这份文件记录会变化的项目现状。长期施工规则以仓库外层的 `AGENTS.md` 和禾禾当轮要求为准。

## 新窗口先做什么

请先完整阅读本文件和当前可用的 `AGENTS.md`，再检查真实仓库状态，不要只凭同名目录判断项目位置。

```powershell
Set-Location -LiteralPath 'D:\st-theater'
git status --short --branch
git log -1 --oneline
```

真实施工仓库是 `D:\st-theater`。Codex 新窗口有时会落在 `C:\Users\lenovo6\OneDrive\文档\st-theater`，该同名目录目前不是正在维护的仓库，不要在那里继续施工。

建议禾禾在新窗口使用这句话开场：

> 请先完整阅读 `D:\st-theater\PROJECT_STATUS.md` 和 AGENTS.md，核对仓库后继续 st-theater 的开发。

## 当前稳定快照

- 产品：SillyTavern 第三方扩展“千夜浮梦 / 小剧场生成器”
- 仓库：`D:\st-theater`
- 远端：`https://github.com/koichole213-ui/st-theater.git`
- 施工分支：`codex/long-dream-preview`（从 `main` 的 `8d97011` 分出，仅供酒馆验收，不视为正式发布）
- 版本：`v3.6.3`
- 最新提交：`8d97011`（`fix: improve fullscreen backups and continuation flow`）
- 远端状态：正式分支 `main` 与 `origin/main` 一致；本轮只推送独立测试分支，不改正式 `main`
- 工作区：正在施工“长梦续章”，尚未提交、尚未推送；修改/新增见下方“长梦续章施工状态”。原有未跟踪目录 `standalone-scripts/` 保持未动；`long-dream-payload.js` 已按本节阶段 2 边界完成纯函数设计与测试，但尚未接入实际 API。既有主 API、独立 API、SSE/非流式读取、429/空流恢复已从 `index.js` 提取到 `api-runtime.js`，普通生成仍经薄适配层调用。两个临时 `.codex-long-dream-*-preview.html` 已在静态验收后删除，不属于正式代码
- 自动测试：`79/79` 通过
- JavaScript 语法检查：通过

## 已定稿的核心产品逻辑

### 1. 字数目标与请求轮次

- 单轮输出上限默认保留 `16384 Token`；若模型不接受该上限，按标准档位自动降低并重试。
- 模型首轮必须明确知道目标正文字数，但不要求模型自报或统计字数。
- 程序统计“可读正文”字数，排除 HTML、CSS、JavaScript、注释和排版代码。
- 用户指令里有明确字数时自动识别；“独立设置目标字数”默认关闭，开启后覆盖指令中的字数。该设置面板可以折叠。
- 没有明确目标字数时只生成一轮，不触发自动补写。
- 自动补写默认开启，默认最多 3 轮，可在设置中调整为 1–10 轮。
- 达到目标的 `90%` 视为完成。
- 若动态收束轮已经获准收尾，模型正常完成结局但仍不足 90%，任务直接结束，不再强行续写；界面显示“已完成，约 X 字，低于目标 X 字”。若该轮是被 Token 截断，则仍可在轮数范围内继续。

当前实际分段策略：

- `< 5000 字`：保持原方式，正文和 HTML 可在同一生成流程中完成。
- `5000–7999 字`：先生成完整纯正文，再单独请求最终 HTML 排版。
- `>= 8000 字`：开启自动补写且允许至少两轮时，正文固定最多两轮；上半篇纯正文停在剧情中段，下半篇承接并完成，最后再独立请求 HTML 排版。若用户关闭自动补写，则不会强行拆成上下篇。

注意：代码中的写作舒适度分类边界与渲染分段边界不是同一件事。`length-policy.js` 中的 `SHORT / COMFORT / LONG` 用来控制文案和节奏；真正决定“正文与 HTML 分离”的阈值是 `5000`，决定“上下篇”的阈值是 `8000`。

### 2. 自动补写与手动续写

- 自动补写请求只携带上一段结尾原文和当前字数计划，不携带完整 A+B，不携带原始长指令，不重新打开 HTML 结构。
- 手动点击“续写”时，只从当前显示结果提取纯正文；绝不把 HTML、CSS 或脚本带进下一次请求。
- 连续手动续写只携带上一轮正文，不累计更早轮次；前情最多保留最近 `8000` 字，过长时截取后半段。
- 续写模型只应输出新增正文，不复述前文。
- v3.5.2 的关键修复是：续写生成期间即使实时区展示的是流式正文，最终结果仍应保留并恢复为 HTML，而不是被纯文字覆盖。
- v3.6.2 起，续写前情会先清理 HTML 排版产生的多余空格和换行；界面、诊断与 8000 字截取统一按可读正文统计。
- 用户本轮指令现在位于前情之后、最终渲染与创作规则之前，对齐酒馆“当前消息后仍有最终指令”的顺序。

### 3. 最终 HTML 排版

- 长篇先写正文再排版，不能以“稳定”为理由长期降级成普通纯文字或简陋 HTML。
- 最终排版请求使用段落占位方案，要求每个正文段落恰好出现一次、顺序不变、位于可见文本节点中。
- 程序检查排版后字数，避免漏段、乱序、隐藏正文、重复正文或模型擅自改写。
- 最终 HTML 校验失败会自动再试一次；仍失败时保留已经生成的正文，不能丢稿。

### 4. 世界书

- 世界书蓝灯/绿灯模式直接复用 SillyTavern 原生扫描逻辑，而不是插件自己仿造关键词判断。
- 蓝灯：常驻触发；绿灯：关键词命中后触发；链式/其他策略由酒馆规则处理。
- 插件只把用户勾选的候选条目交给酒馆扫描，再接回本轮真正触发的结果。
- 酒馆扫描不可用时有回退路径，并写入脱敏运行日志。

### 5. 上下文、模板与移动端

- 聊天前文楼层数改为数字输入，允许 `0–500`；填 0 表示完全不读取聊天消息。
- 指令模板的 JSON 导出/导入会保留文件夹分组和空文件夹；兼容旧 TXT 批量导入及 Windows 换行。
- 世界书和模板列表的手机布局已经压缩重排；顶部重复的“全选 / 全不选”已移除。
- 魔法棒菜单入口已固定排序，避免每次刷新在其他扩展之间忽上忽下。

### 6. API、空流、429 与日志

- 支持酒馆主 API 和独立 API；独立 API 支持自动/OpenAI/Anthropic 格式及流式/非流式兼容。
- 不要把 429 文案写死成“公益站限流”。统一说明为请求过于频繁或当前线路繁忙，并按响应里的 `Retry-After` 等信息处理重试。
- `Stream empty` 会尝试识别流内错误、空流和可恢复情况；若后续轮出错，必须保留此前生成的正文。
- 运行日志只保存在内存最近 200 条；复制前统一隐藏 API Key、Authorization、敏感 URL 路径等信息。
- “复制日志”已加入多级剪贴板兜底与人工复制弹窗，不能在没有真正写入剪贴板时只提示“已复制”。
- 所有新增日志都按“可能被用户直接公开转发”的标准处理，禁止记录或回显密钥。

### 7. 阅读模式

- 纯文字模板分为亮色与暗色夜读，两者都只要求模型输出正文，配色由插件本地完成。
- 生成区支持全屏阅读，可显示 HTML 小剧场及两种纯文字主题，并支持按钮或 Esc 退出。
- 全屏阅读使用浏览器顶层弹窗，不会再被酒馆的插件窗口压在背后。
- HTML 无法正常显示时会保留正文并切换到安全的纯文字展示，避免生成完成后只剩空白。

### 8. 长梦续章（第一阶段骨架）

当前产品决定：长梦续章是独立于普通生成页和普通“续写”的长篇支线入口，不把更多控件继续塞进已经拥挤的生成界面。

已经实现但尚未提交：

- 顶部新增独立“长梦”入口，包含长卷列表、开启长梦的“定梦”流程和长卷详情页；视觉采用夜色档案 / 纸页长卷方向，并有手机窄屏适配。
- 可以从当前正在查看、最近生成或历史记录中选择一篇作为第一章；首章同时保存完整 HTML、可读正文、原指令、输出模式和来源快照，不会牺牲已有 HTML 丰富度。
- 新增长梦数据模块 `long-dream.js`，维护长卷、章节、世界线定义、来源配置快照及未来“梦脉”容器。
- IndexedDB 从版本 1 升到版本 2，新增独立 `dreams` 仓库；长梦不会和普通历史混存。IndexedDB 不可用时仍回退到 settings。
- “定梦”由用户明确确认，不依赖 API 猜关系：默认“只沿用此梦世界线”，不会记录任何可自动继承的原世界书；用户也可以显式选择“沿用当前选中的世界书”。
- 详情页可以修改长卷名字、此梦设定和世界书继承策略；修改定义不会改写或丢失已经保存的章节。
- 可以从长卷详情打开最新章节回到现有阅读区，也可以删除整部长卷；普通历史不受影响。
- 详情页已加入“续写下一章”的信息层级草案：章名、目标字数、本章方向和预计读取范围；按钮保持禁用并明确显示“等待接入”，当前不会发起网络请求或自动追加章节。
- `long-dream.js` 的 schema 已升到 2，补入追加/更新章节、从指定章节截断后续、完卷状态、草稿保存/清理/确认晋升和旧记录迁移。正式章节必须同时具备纯正文与最终 HTML；草稿不会被当成正式章节。
- 明确沿用世界书时会冻结实际条目内容和非敏感匹配字段；切回默认隔离会清掉继承快照，避免以后误用旧世界线。原世界书后续变化不会静默进入已建立的长梦。
- 新增纯函数模块 `long-dream-payload.js`，只读取长卷自身的定梦、章节、已确认梦脉与允许的冻结快照，不读取聊天前文、普通续写缓存或现场世界书；带可测试的上下文预算，定梦和本章方向不会先于低优先级信息被裁掉。

尚未接入，下一阶段再做：

- “续写下一章”的实际 AI 请求与章节追加。
- “梦脉”提取、确认、压缩与注入策略。
- 长卷章节目录、章节回退/重写、完卷状态。
- 长梦专用导出/导入备份；当前历史 ZIP/JSON 备份不包含独立 `dreams` 仓库。
- 正式发布与版本号更新；当前代码常量、manifest、样式头仍应保持 `v3.6.3`，直到禾禾明确决定发布并 push。

本轮验收边界（2026-07-31）：

- 已用实际长梦 DOM/CSS 做过桌面与 375px 窄屏静态预览：长卷卡片整卡可点击，键盘焦点可见，返回/阅读/主操作按钮最小高度 44px；续章表单在窄屏会变为单列，未发现横向溢出。
- 已能访问禾禾电脑上的 SillyTavern，但该页面当前加载的剧场仍是已发布的 `v3.6.3`，不是 `D:\st-theater` 当前长梦工作树，因此阶段 0 仍未完成真实酒馆验收。需要先把本地工作树部署到该酒馆，再检查默认皮肤、酒馆皮肤、自定义皮肤，以及当前结果/最近生成/历史三种开卷来源。
- 当前没有接入实际 AI 续章、流式显示、停止、排版或正式章节保存闭环；不得把纯 payload 测试描述成阶段 3 已完成。

下一阶段的重要边界：

- API 不负责“智能分清原世界线与 AU 世界线”。程序必须只按用户确认的 `canon` 和继承策略组装请求；AI 可以提出设定草稿，但不能静默决定人物关系。
- 默认分支隔离时，后续请求不得重新扫描或注入原世界书；只有用户明确选择沿用时，才使用长卷内已经冻结的世界书内容快照，不能只凭书名现场重读。
- 长梦续写要消费整部长卷自己的章节与梦脉，不能复用普通续写“只带上一轮 8000 字”的语义，也不能改变普通续写的既有行为。

#### 8.1 产品定位与明确不做的事

长梦续章解决的是“同一个小剧场连续续写很多次以后，人物关系、剧情因果、伏笔和细节逐渐混乱”的问题。它不是把整个 SillyTavern 重新做进插件，也不是“小手机 2.0”。

- 普通小剧场继续负责一次性生成和短续写；长梦只服务于已经决定长期连载的某一场小剧场。
- 长梦使用独立入口，是因为生成页的基本信息已经接近视觉容量上限。长梦的章节、记忆和世界线控件不得重新塞回普通生成页。
- 普通“续写”仍保持点击后自动回到生成页顶部并聚焦指令框；不要为了长梦改坏这条短续写路径。
- 不在长梦里复刻酒馆全部预设插入位、严格工具协议、角色前/角色后、chat history 排序或完整世界书引擎。确有价值的兼容问题可以单独修，但不能让小剧场扩张成微型酒馆。
- 不整包合并他人的大改版；其中确有价值的问题应逐项验证、独立实现。对方若希望保留完整的微型酒馆路线，可以自行维护二改分支，不要求主版全部吸收。
- 不引用或搬运尚未取得作者授权的“小手机”代码。可以独立讨论“古法总结”等通用思路，但实现必须由本项目自行设计。
- 不读取或修改酒馆里未由用户选择的 API 配置，不偷偷切换正文使用的 Key，也不在长梦记录、日志或备份中保存 API Key。长梦只通过插件现有、界面上可见的 API 路由发请求。
- 不把原聊天几千楼的长期记忆系统原样搬来。长梦的记忆单位是“章节”，不是酒馆楼层；应针对小剧场连载设计更轻、更透明的方案。

#### 8.2 核心概念和数据职责

- **长卷（dream）**：一条独立连载世界线，包含名字、状态、创建时间、此梦设定、世界书继承策略、章节和梦脉。
- **章节（chapter）**：每次确认保存的一次续写结果。必须同时保存纯正文与最终 HTML；纯正文供上下文和检索，HTML 供原样阅读与导出。
- **定梦（canon）**：由用户确认的这条支线的硬事实，例如“没有血缘关系，是青梅竹马”。它不是普通写作指令，也不应由 AI 静默修改。
- **梦脉（memory cards）**：从章节中提取的结构化剧情记忆，用来在章节很多以后找回人物状态、事件、伏笔和关键原话；它不能替代章节原文。
- **来源快照（source/sourceConfig）**：记录第一章来自当前结果、最近生成还是历史，以及当时使用的模式、指令、预设名称等。只记录复现所需的非敏感信息，不保存 Key。
- **继承快照（future world-book snapshot）**：若用户明确选择沿用世界书，正式接入续章前应冻结当时允许继承的内容或版本摘要，而不只保存书名。此后原世界书变化不应悄悄改写已经成立的长梦；刷新继承必须由用户主动确认。

建议后续为每张梦脉保留这些字段：

- `type`：事件、人物状态、关系变化、地点/物品、伏笔、约定、关键原话等。
- `content`：短而明确的事实，不写空泛文学总结。
- `chapterId` / `chapterNumber`：可以回到原章节核对。
- `quote`：确有价值时保留很短的原句或对话片段，避免古法总结丢掉细节口吻。
- `status`：待确认、已确认、已废止；AI 新提取的内容不能天然等同于用户确认的定梦。
- `tags`：人物、地点、物品或伏笔关键词，为后期检索服务。

#### 8.3 世界线隔离与请求优先级

API 本身不会可靠地知道“原世界书中的兄妹”和“长梦中的青梅竹马”哪一个才是本次事实。可靠性来自程序确定性地分层、裁剪和标注上下文，而不是期待模型自行领悟。

后续续章请求应按以下优先级组装：

1. 用户本次明确写下的章节方向，但不能在无提示的情况下推翻已经确认的定梦。
2. 用户确认的 `canon`，作为最高级的持续世界线事实。
3. 长卷已经保存的章节事实和已确认梦脉。
4. 用户主动允许的冻结世界书快照；默认分支隔离时这一层完全不存在。
5. 写作预设、风格附加规则和渲染规则；这些只控制表达，不得改写事实。

冲突处理原则：

- `canon` 与原世界书冲突：以 `canon` 为准，并且不要把冲突的原条目发给模型。
- 新章节方向与旧定梦冲突：界面提示用户选择“修改定梦”或“保留旧设定”，不能由模型暗中圆场。
- 已保存章节彼此出现矛盾：先标为待处理冲突，不自动删除任何章节或梦脉。
- 用户从“只沿用此梦世界线”切换成“沿用世界书”时，需要明确显示准备加入哪些内容；反向切换时停止后续注入，但不改写过去章节。

#### 8.4 梦脉为何不能只做一段古法总结

把第 1–300 条内容总结成一段话再勾选注入，优点是简单、省 Token；缺点是会压掉细节对白、物件去向、人物细微态度和未兑现伏笔。对长梦而言，只有一段滚动摘要不够。

暂定采用“原文章节 + 结构化梦脉 + 近期窗口”的混合方案：

- 章节不多时，直接携带全部章节纯正文，不急着总结，优先保证细节完整。
- 每生成并保存一章后，可用一次副请求提取候选梦脉；候选内容先可见、可改，之后才成为确认记忆。
- 章节增长后，保留最近若干章全文；更早章节以已确认梦脉为主，需要时再按人物、地点、物品和本章方向检索相关记忆。
- 检索数量应有限制，初步可以沿用禾禾长期记忆脚本中“最多约 20 条相关纪要”的经验，但长梦要按章节和剧情相关性选择，不照搬“每三楼总结一次”。
- 对关键对白、约定、伏笔原句，梦脉保存短引用和章节定位；需要更多细节时仍能回看原章，不能只剩摘要。
- 梦脉提取失败不应阻止章节保存；记忆是增强层，章节原文才是不可丢的底稿。

#### 8.5 目标用户流程

1. 用户在普通生成区得到一篇满意的小剧场，或从历史里打开一篇旧作。
2. 进入独立“长梦”页，选择这篇作品作为第一章。
3. 在“定梦”中确认长卷名、支线硬设定以及是否沿用选中的世界书；默认隔离原世界书。
4. 长卷详情显示最新章节、章节数量、梦脉数量和世界线状态。
5. 用户点击“续写下一章”，填写可选章名、目标字数和本章方向；页面应明确显示本次会读取哪些上下文。
6. 模型只返回新增章节正文；程序完成现有安全排版后，先让用户阅读，再原子性保存正文与 HTML 为新章节。
7. 保存成功后异步提取候选梦脉。用户可以稍后确认，不强迫每章都打断阅读流程。
8. 用户可在章节目录中阅读旧章、重写某章、从某章另开分支或回退；这些操作必须有明确影响范围，不能静默覆盖后续章节。
9. 完卷后可以导出整部长梦；再次导入时恢复定梦、章节、梦脉、继承快照和原始 HTML，但不包含任何密钥。

#### 8.6 详细开发顺序与每阶段完成标准

**阶段 0：验收第一阶段骨架**

- 在真实 SillyTavern 中检查顶部入口、空状态、定梦、长卷列表、详情页和“阅读本章”。
- 分别检查默认皮肤、酒馆皮肤、自定义皮肤，以及手机窄屏、非全屏和全屏阅读。
- 用当前结果、最近生成、历史记录各开一卷，确认不会错带上一轮正文或指令。
- 完成标准：禾禾确认信息层级和操作路径自然，普通生成、历史和普通续写没有回归。

**阶段 1：稳固章节数据层**

- 在 `long-dream.js` 中加入追加章节、更新章节、删除/截断后续章节、完卷状态和数据迁移函数。
- 保存新章节时使用完整对象校验；正文和 HTML 必须同一次事务成功，失败时保留旧长卷。
- 设计“生成中的草稿”恢复，但草稿不能冒充已经确认的正式章节。
- 为明确沿用世界书的长卷增加非敏感继承快照或版本摘要。
- 完成标准：刷新页面、数据库回退、异常中断后都不会丢已保存章节，也不会产生半章。

**阶段 2：建立长梦专用上下文组装器**

- 单独建立类似 `long-dream-payload.js` 的纯函数模块，不把长梦条件继续堆进普通 `generation-payload.js`。
- 输入只接受当前长卷、用户本章方向、明确允许的世界书快照、当前插件 API/预设选择和目标字数。
- 输出明确分区：此梦设定、允许继承的世界书、梦脉、历史章节、当前章方向和输出协议。
- 默认隔离时不得调用原世界书扫描；不得读取酒馆聊天前文；不得带入普通续写缓存。
- 添加冲突和 Token 预算测试，保证定梦永远不会在裁剪时先于低优先级信息被删掉。
- 完成标准：通过单元测试可以证明，同一份原世界书在“兄妹原作 / 青梅竹马长梦”场景下不会进入错误请求。

**阶段 3：接入“续写下一章”生成闭环**

- 在长卷详情中提供独立续章面板：章名、目标字数、本章方向、将读取的章节/梦脉/世界书状态。
- 复用已经稳定的 API 客户端、流式显示、停止、429/空流恢复、字数统计和最终 HTML 排版；不复制第二套网络实现。
- 网络运行能力已在阶段 3 前提取到 `api-runtime.js`；接线时由长梦传入自己的 payload、停止信号和流式回调，普通生成与长梦共用该模块。
- 请求语义是“新增一章”，不是普通短续写；不得把旧 HTML/CSS 发给模型，也不得重复旧章节。
- 生成失败或用户停止时先保留可恢复草稿；只有正文与最终显示结果有效后才追加正式章节。
- 新章只进入当前长卷，不自动塞进普通历史或最近三次生成；若用户想另存普通历史，应显式操作。
- 完成标准：连续生成至少 5 章，人物关系、章节顺序和 HTML 阅读均正确，失败重试不会重复追加。

**阶段 4：加入 AI 定梦建议，但保留用户主权**

- AI 可以根据第一章提出“人物关系、时间地点、已发生事件、不可违反事实”的设定草稿。
- 草稿逐项展示，用户可以修改、删除、接受；未确认内容不能写入 `canon`。
- AI 不确定时应标注“不确定/需要确认”，不使用看似聪明的自动归类掩盖歧义。
- 完成标准：不开启 AI 建议也能正常开卷；接受建议前后有清晰差异，且永远不会静默覆盖用户手写定梦。

**阶段 5：接入梦脉提取和校正**

- 每章保存后异步提取候选记忆，类型至少覆盖事件、人物状态、关系变化、伏笔、物品/地点和关键原话。
- 候选梦脉可批量确认，也可编辑、废止和跳过；不强迫用户每章立刻整理。
- 提取请求只使用新章和必要旧状态，不重复发送整部长卷，控制副 API 成本。
- 失败时写脱敏诊断并允许稍后重试，不影响章节阅读或下一章生成。
- 完成标准：梦脉能追溯到原章；错误记忆可以撤销；旧梦脉不会因一次新提取被无声覆盖。

**阶段 6：处理真正的长连载记忆**

- 设定近期全文窗口；窗口外按本章方向检索最相关的已确认梦脉，数量和 Token 均设上限。
- 始终保留定梦、未解决伏笔、当前人物状态和关键约定；普通气氛描写优先降级。
- 增加记忆命中预览或诊断，让用户知道本章实际带了哪些梦脉，而不是黑箱“超强记忆”。
- 对几百章场景再评估索引和压缩，不在数据规模尚小时提前引入复杂向量库。
- 完成标准：构造几十章测试时，早期关键事件可被正确召回，同时请求体不会无上限增长。

**阶段 7：章节管理与分支操作**

- 增加章节目录、旧章阅读、重命名、重写、删除、从某章另开长卷和完卷。
- 重写中间章节时必须提示后续章节可能失效；可以选择保留原分支或从该章截断，不能直接覆盖整条时间线。
- 删除和截断需要二次确认，并优先提供可恢复方案。
- 完成标准：所有会影响后续章节的操作都能在执行前说明范围，且原分支不会意外丢失。

**阶段 8：备份、兼容和发布**

- 设计长梦专用 ZIP：清单保存定梦、章节元数据、梦脉和继承策略，每章 HTML 独立存放，并可选择附带纯正文。
- 导入时校验 schema、文件缺失和重名；兼容未来版本迁移，不能让普通历史 ZIP 误当成长梦导入。
- 做真实移动端、不同皮肤、不同 API 协议、刷新恢复、数据库失败和大体量存档验收。
- 最后才同步更新 `VERSION`、`manifest.json` 和 `style.css` 文件头，整理公告、commit 并在禾禾明确要求后 push。
- 完成标准：备份跨设备可恢复，所有自动测试和人工清单通过，发布包中不含 Key、调试预览或私人数据。

#### 8.7 实施时必须持续检查的风险

- **上下文爆炸**：不能永久把全部章节全文塞进请求；先保证正确，再在真实规模出现后逐步引入窗口和梦脉检索。
- **总结失真**：梦脉只能帮助找回信息，不得覆盖原章节；所有事实应能追溯到来源。
- **世界线回流**：任何对酒馆世界书和聊天前文的读取都必须经过长梦继承策略，默认路径为不读取。
- **普通功能回归**：长梦生成状态、最近生成索引和普通 `continueContext` 必须相互独立。
- **HTML 丢失**：存储、重写、备份和全屏阅读都必须保留完整 HTML，不以纯文字降级换表面稳定。
- **密钥泄漏**：长梦只记录 API 预设名称等非敏感引用；日志、诊断和导出继续执行现有脱敏规则。
- **范围膨胀**：每一项新需求先判断是否直接提高小剧场连载的连贯性、可控性或可恢复性；若只是复刻酒馆原生能力，原则上不进入主版。

## 近期提交脉络

从新到旧：

- `8d97011`：发布 v3.6.3，修复全屏 HTML、历史 ZIP 导入和续写自动回到指令框
- `db5cda4`：对齐用户指令顺序，统一续写字数显示与前情截取口径
- `f620a33`：修复全屏阅读被插件窗口遮挡
- `29133f5`：加入暗色夜读与全屏阅读
- `1f6354a`：续写流程保留 HTML 结果，且续写请求不携带旧 HTML
- `e597753`：空流恢复与 429/繁忙处理
- `96c9407`：发布 v3.5.0，增强最终渲染与日志复制
- `bfb83db`：调整分段生成阈值
- `29954e4`：加入面向目标字数的长篇生成计划
- `129c341`：固定魔法棒菜单入口位置
- `f03c02e`：复用酒馆原生世界书激活逻辑
- `52fd173`：修复酒馆主 API，并压缩移动端 UI
- `b6c5f5c`：发布 v3.4.0

## 当前验收重点

每次动到生成、字数、续写或渲染逻辑后，至少覆盖这些场景：

1. 同一角色卡、同一条 `8000 字`指令连续跑 3 次，记录每次实际可读字数和请求轮数；三次均 `>= 6000 字`才算通过。
2. `3000 字`：通常应能单轮完成，确认模型首轮收到目标字数，且 HTML 丰富度正常。
3. `5000 / 6000 / 7000 字`：确认先得到完整纯正文，再单独排版；6000 字不能退化成普通简陋 HTML。
4. `8000 字`：确认正文按上下篇承接，最后统一排版；下半篇不能复述上半篇。
5. 构造“收束轮正常完成但不足 90%”场景，确认结局后不再发请求。
6. 构造第二轮 `Stream empty` 或 429，确认第一轮正文仍被保留，提示不归因于某类站点。
7. 手动连续续写两次，检查请求中只有上一轮纯正文，没有 A+B 全文，也没有 HTML/CSS/脚本；最终结果仍为所选 HTML 样式。
8. 世界书分别测试蓝灯常开、绿灯关键词命中/未命中，以及手机窄屏布局。
9. “复制日志”后粘贴检查内容确实是最新日志，且没有 Key、Token、Authorization 等敏感内容。

## 文件地图

- `index.js`：插件入口、界面、设置、生成总流程
- `long-dream.js`：长梦数据模型、规范化、定梦更新与最新章节读取
- `long-dream-payload.js`：长梦专用纯请求组装、冻结世界书隔离与上下文预算；尚未接入实际 API
- `length-policy.js`：目标字数解析、分档、首轮篇幅引导
- `generation-job.js`：轮次、90% 完成线、动态收束与停止条件
- `generation-payload.js`：首轮、续写和最终 HTML 请求内容
- `text-counter.js`：可读字数统计、续写上下文窗口
- `api-client.js`：API 请求构造、协议兼容、响应字段与错误类型解析等纯逻辑
- `api-runtime.js`：酒馆主 API/独立 API 调用、SSE 与非流式读取、停止、429 重试、空流恢复和主 API 路径降级
- `runtime-log.js`：内存日志和脱敏
- `safe-renderer.js`：安全 iframe 渲染与尺寸/正文上报
- `world-book-scan.js`、`world-book-policy.js`：酒馆世界书扫描接入与条目策略
- `style.css`：插件样式与移动端适配
- `settings.html`：设置面板模板
- `tests/core.test.js`：核心回归测试

## 修改与发布规则

- 修改前先运行 `git status --short --branch`，保留禾禾已有或无关改动。
- JavaScript 改动至少运行：

```powershell
node --check .\index.js
node --check .\api-client.js
node --check .\api-runtime.js
node --check .\safe-renderer.js
node --check .\request-metrics.js
node --check .\persona-follow.js
node --check .\notify.js
node --test --test-isolation=none .\tests\core.test.js
```

- 发布新版本时同步修改 `index.js` 的 `VERSION`、`manifest.json` 的 `version`、`style.css` 文件头版本，并确认设置页底部使用 `VERSION` 显示最新版。
- 禾禾没有明确说“push / 推送”时，不主动提交或推送。
- 推送后报告：版本号、分支、提交哈希、测试结果、工作区是否干净。
- 不提交 API Key、Token、Authorization、密码或用户日志中的敏感内容。

## 下一窗口的工作边界

- 当前稳定版本 `v3.6.3` 已推送；工作区另有“长梦续章”骨架、阶段 1 数据基础、阶段 2 纯请求设计，以及为阶段 3 准备的 API 运行层提取，尚未提交和推送。涉及文件：`index.js`、`style.css`、`tests/core.test.js`、新文件 `long-dream.js`、`long-dream-payload.js`、`api-runtime.js`，以及本交接文件。原有未跟踪目录 `standalone-scripts/` 保持未动。
- 两个 `.codex-long-dream-*-preview.html` 是本轮临时静态验收文件，已经删除，不能进入正式发布包。`long-dream-payload.js` 已完整审查并通过测试，但仍未从 `index.js` 调用，不会发起请求。
- 新窗口先核对上述 diff，不要重写已经完成的骨架，也不要另造网络实现。先把当前工作树部署到真实 SillyTavern，让禾禾查看入口、定梦页、长卷列表、详情页和禁用状态的续章面板；完成阶段 0 后，再通过 `api-runtime.js` 把现有纯 payload 与流式/停止/排版能力接成阶段 3 闭环。
- 当前全量语法检查通过，`node --test --test-isolation=none .\tests\core.test.js` 为 `79/79` 通过；新增覆盖 SSE 累积、429 单次重试、空流转非流式，以及酒馆主 API 的 ChatCompletionService/TavernHelper 两条路径。
- 当前没有版本升级，也没有本轮 commit / push；只有禾禾明确要求后再发布。
- 新反馈若涉及“字数不够”，先让禾禾提供复制后的脱敏运行日志，区分模型自然收尾、轮次策略、空流/429、最终排版失败，不要直接再次堆叠提示词。
- 新反馈若涉及“最终变纯文字/普通 HTML”，优先检查 staged render、最终占位校验和续写后的结果恢复，不要用关闭美化作为长期修复。
- 不要重新推翻已经定稿的 16384、90% 完成线、5000/8000 两个分段阈值和“续写只带上一轮纯正文”原则，除非禾禾明确决定更改产品方向。
