# st-theater 项目交接

> 最后核对：2026-08-19（Asia/Shanghai）
> 这份文件记录会变化的项目现状。长期施工规则以仓库外层的 `AGENTS.md` 和禾禾当轮要求为准。

## 紧急交接：v4.1.1 悬浮球防误触与设置页稳定（2026-08-19）

本节是当前权威状态；下方标注 2026-08-09 及更早的内容只保留为历史记录，不要用旧版本号、旧测试数或旧工作区状态覆盖本节。

- 正确施工 worktree：`D:\st-theater\.codex-long-dream-ui`
- 本地施工分支：`codex/long-dream-ui-tune`
- 远端预览分支：`origin/codex/long-dream-preview`
- 插件版本：`v4.1.1`（`index.js`、`manifest.json`、`style.css` 与设置页一致）
- 本轮改动：悬浮球拖动重新明确独占触摸手势并持续跟随最先按住的手指，页面滚动、第二根手指和旧手机补发动作不会再抢走或打断拖动。
- 本轮改动：悬浮球在手指抬起后会先吞掉同一触点补发的点击，再打开弹窗，避免打开设置页时误触当前位置下方的 API 删除等按钮。
- 本轮改动：悬浮球重建或拖动被系统中断时会清理旧计时器、拖动监听与屏幕监听，反复开关不会累积重复动作。
- 本轮改动：设置页六个区域会在弹窗第一次出现前直接按最终卡片布局生成，API 预设的四个管理按钮不再先显示旧排列再换位。
- 本轮改动：待确认新章的候选切换器改为无边框、无底色的小号纯文字样式，并在整行中真正居中；全屏键仍固定在右侧。
- 本轮改动：放弃重写、确认保存、按原要求再生成一版三个操作的图标与文字整体居中，操作区也沿卡片中轴对齐。
- 本轮改动：手机端生成指令框由 80px 增至 120px，长梦续写指令框由 122px 增至 183px；电脑端尺寸不变。
- 本轮改动：弹窗在显示前直接按上次标签生成，不再先显示写死的“生成”页、等资料加载后再跳回设置页。
- 上一提交：`2af50f6 release: publish v4.1.1`
- 自动检查：`git diff --check`、`node --check .\index.js` 通过；核心测试 `187/187` 通过。

本轮已经完成：

- 手机端主插件外壳在 `768px` 以下使用 `100vw × 100dvh` 铺满网页可用区域；电脑端保持原弹窗，默认/跟随酒馆/自定义三种皮肤不重做内部 UI。
- 保留 SillyTavern 原有底部 `Close`，不新增右上角 `×`。底栏上下留白从旧的厚间距压缩为 `4px`，按钮仍保持至少 `44px` 的触控高度。
- 手机端“生成”和“长梦”主指令框都在原高度基础上增加 50%，长指令更容易查看和修改。
- 插件会在弹窗首次绘制时直接恢复上次标签；设置页等非生成页不再闪过生成界面。
- 每个酒馆预设分别保存自己的条目勾选状态；切换、重新打开、单项勾选、全选和全不选都会恢复当前预设自己的记录。旧版公共记录会迁移给升级时正在使用的预设，不修改酒馆预设文件。
- 长梦待确认页的字数统计按整个标题区居中；最终 HTML 排版进度统一由一处刷新，不再让“正文已完成”和“已接收多少字符”交替闪烁。
- 放弃长梦本轮草稿继续保留续写指令；候选切换与全屏按钮保持同一行；旧历史开卷的世界线选择不再因世界书尚未同步而锁死。

下一步只需要禾禾在真实手机 SillyTavern 更新后复测：先确认悬浮球可以顺滑长距离拖动和贴边，再把它放到 API 删除按钮附近点击，确认只会打开弹窗而不会弹出删除确认；同时确认设置页四个管理按钮从第一眼起就保持最终横向排列。此前的长梦审阅对齐、指令框高度、启动页恢复、铺满和紧凑 `Close` 继续保留。

## 新窗口先做什么

请先完整阅读本文件和当前可用的 `AGENTS.md`，再检查真实仓库状态，不要只凭同名目录判断项目位置。

```powershell
Set-Location -LiteralPath 'D:\st-theater\.codex-long-dream-ui'
git status --short --branch
git log -1 --oneline
```

当前长梦预览的正确施工目录是 `D:\st-theater\.codex-long-dream-ui`。`D:\st-theater` 是另一个已注册 worktree，可能包含禾禾的其他改动；不要把两个 worktree 的分支或未提交文件混在一起。Codex 新窗口若落在 `C:\Users\lenovo6\OneDrive\文档\st-theater`，该同名目录不是正在维护的仓库。

建议禾禾在新窗口使用这句话开场：

> 请先完整阅读 `D:\st-theater\.codex-long-dream-ui\PROJECT_STATUS.md` 和 AGENTS.md，核对 `codex/long-dream-ui-tune`、`origin/codex/long-dream-preview`、当前提交与工作区状态后继续 `v4.1.1` 真实手机验收。重点复测悬浮球拖动是否顺滑、打开时是否不会误触其下方的 API 删除按钮，并确认设置页按钮不闪动、长梦审阅操作仍然居中；不要改动其他 worktree，也不要继续施工或推送，除非禾禾当轮明确授权。

## 当前稳定快照

- 产品：SillyTavern 第三方扩展“千夜浮梦 / 小剧场生成器”
- 仓库：`D:\st-theater`
- 远端：`https://github.com/koichole213-ui/st-theater.git`
- 施工分支：`codex/long-dream-preview`（从 `main` 的 `8d97011` 分出，仅供酒馆验收，不视为正式发布）
- 版本：`v3.6.4`
- 当前 HEAD：`codex/long-dream-preview` 最新提交（精确哈希以 `git log -1 --oneline` 为准）
- 远端状态：本轮完成后 `codex/long-dream-preview` 与 `origin/codex/long-dream-preview` 同步；正式 `main` 未改动
- 工作区：本轮提交不包含原有未跟踪目录 `standalone-scripts/`；该目录始终未改动、未暂存
- 自动测试：`160/160` 通过
- JavaScript 语法检查：通过

## 最新施工：统一长梦基础注入、冻结勾选条目与实时 Token（2026-08-09）

- 普通生成与长梦现在共用同一份人物身份解析：长梦每一轮都会按当前设置注入所选酒馆预设、Char 描述与性格、User 人设、文风/NSFW 补充和主角锚点。完全隔离 AU 只排除原作场景与世界书，不再清空人物性格；若角色卡资料与定梦冲突，定梦和已保存章节优先。
- 长梦世界书不做关键词触发推断。新开卷只冻结素材页当前明确勾选的条目，续章完整保留这些条目的位置、深度、角色、顺序和 Outlet；用户明确勾选的冻结资料不受可选上下文预算裁剪。
- 旧长梦不会被静默改写。长梦设置新增“用素材页当前勾选更新冻结资料”，明确显示当前条目数并在确认后替换旧快照；用于把已有的 33 条快照安全更新成当前实际勾选的 5–7 条，正式章节不变。
- 长梦第一轮读取定梦、近期章节、梦脉和冻结资料；同章第二轮及中断恢复轮仍保留预设、Char/User、定梦、梦脉与冻结资料，但只带本章正文最近 4000 字，不再重复整部长卷章节前情。可替代上下文启用 32000 字符安全预算。
- 续章输入区新增实时 Token 估算，按实际结构化消息和预设后处理计算，并拆分显示预设、Char/User、冻结世界书条目数与 Token、长梦前情及规则。长梦流式首字现在也写入诊断计时。
- 候选版本切换 `< 1/3 >` 整组在桌面与手机端居中，全屏按钮仍靠右；梦脉容器不继承居中，当前脉象、内容文本框、引用和历史正文改为两端对齐。本章梦脉命中计数同时修正了把数字误当数组读取的问题。
- 版本同步升级为 `v3.6.4`：`index.js`、`manifest.json`、`style.css` 文件头一致。新增 2 项行为回归与界面静态断言，核心测试 `160/160` 通过。

## 最新施工：长梦续写复用普通生成线路并降低流式负载（2026-08-09）

- 禾禾反馈长梦续写偶发不出字、接近结尾时停住、等待很久，以及停止主 API、切换独立 API 后仍看到酒馆小飞机被占用；普通生成页的生成、续写与线路切换实际使用正常，因此本轮明确以普通生成链路为基准，不再为长梦另造平行请求体系。
- `index.js` 已从普通生成现有逻辑中提取共用线路入口：普通生成、普通续写、长梦正文、自动补写和最终 HTML 排版都使用同一函数。任务开始时冻结当前主/独立 API 选择和独立 API 配置，同一任务的各轮与排版不会再次各自读取全局线路；停止后重新生成会重新取得当前选择。
- 普通生成原有的 100ms 流式节流与增量追加已抽成共用渲染器，长梦不再每个流片段都覆盖整段 DOM。长梦草稿检查点从 750ms 调整为 3000ms，并把并发中间快照合并成最新一份；轮次结束、停止和排版前仍强制等待最新草稿写入，保留防丢能力。
- 共用独立 API SSE 读取新增 60 秒无新数据保护；连接保持但不再继续传输时会返回 `T-NET-TIMEOUT`，长梦控制器随后保存已收到的正文草稿，不会无限卡在“仍在生成”。该保护位于既有 `api-runtime.js`，不是长梦专用副本。
- 新增 4 项回归：同一长梦正文各轮和排版沿用同一线路快照；高频流只合并保存最新草稿；共用独立 API 流停顿后结束等待并保留已收正文；普通生成、长梦正文和排版必须引用同一线路入口。当前核心测试 `158/158`、三个改动 JavaScript 文件语法检查和 `git diff --check` 均通过。
- 尚未在真实 SillyTavern 验收；重点复现顺序为：主 API 开始长梦 → 停止并等界面恢复可继续 → 切独立 API → 继续未完成草稿/重新生成 → 关闭弹窗观察酒馆小飞机是否释放，同时验证长篇后半段持续出字。
- 本轮没有升级 `v3.6.3`，没有提交或推送。

## 最新施工：梦脉 v2 首轮实现完成，本地待真实酒馆验收（2026-08-08）

- 禾禾确认按小砚对小澄方案的补充建议推进。新增冻结规格 `DREAM_MEMORY_V2_SPEC.md` 和六章标准样本 `tests/fixtures/dream-memory-v2-standard.json`，明确梦脉是“副 API 提议增量、程序确定性归档、用户最终决定、主 API 只读”的连续性档案。
- 新增 `long-dream-memory-model.js`：梦脉改为 `states / transitions / threads / deviations` 四层；状态保留旧值历史且新值来源不继承旧章，事项保留推进历史与解决/放弃生命周期，世界线偏离在完全隔离模式拒绝生成。
- 副 API 改为固定底线规则与固定 `currentState + operations` 输出合同，只允许七种白名单增量操作。已有记录优先按 `targetId` 更新；同批新建事项可用相同 `threadKey` 继续推进。非法操作可局部忽略，不阻止同批合法操作。
- `currentState` 已降为只读阅读摘要；存在用户锁定、用户否定、已关闭事项重开或目标缺失等冲突时不会静默覆盖旧摘要。冲突持久保存，界面允许“以新章节为准”或“保留我的版本”。
- 用户操作已拆分为校正并锁定、交还自动更新、隐藏不注入、否定错误、事项解决/放弃/重新开启；用户否定会建立抑制记录，后续同一错误不会直接复活。
- 主 API 梦脉检索改为当前状态、开放事项、世界线偏离、关键变化的分层软配额，并继续受总上下文预算保护；v1 兼容卡片保留并可在相关时低优先级命中。
- 旧版梦脉不破坏性迁移：稳定的 `人物状态/关系/地点物品` 槽位会保守转成 v2 状态，原 v1 卡片仍完整保留；无法可靠结构化的内容继续作为兼容记忆注入。JSON/ZIP 白名单备份已保留 v2 四层、状态历史、事项进展、否定和待确认冲突。
- 新增 `long-dream-memory-presets.js` 和设置页“梦脉分析预设库”：内置预设不可覆盖，可新建副本、编辑、导入/导出 JSON、删除自定义预设。二创 JSON 只携带分析侧重点与公开元数据，固定输出合同为 `long-dream-memory-v2`，不携带 API、密钥、长梦正文或用户梦脉；自定义结构包暂未开放。
- 梦脉详情界面延续现有夜色档案风格，按四层折叠展示；技术冲突代码不直接暴露给用户，手机端按钮可单列。尚未在真实 SillyTavern 验收副 API 的实际 operations 质量、复杂长卷的检索结果、预设导入导出和触摸操作。
- 新增 8 项核心回归，当前 `node --test --test-isolation=none .\tests\core.test.js` 为 `154/154`；`index.js`、梦脉数据/提示词/预设/检索/备份模块语法检查、CSS 花括号和 `git diff --check` 均通过。
- 本轮没有升级插件版本，没有提交或推送；远端仍停在 `6190a6b`。

## 最新交接：长梦功能验收完成，转入梦脉 v2 讨论（2026-08-08）

- 长梦剩余功能已在 `6190a6b` 完成并推送测试分支：梦脉人工编辑/废止/恢复、`type + key` 状态原位更新、人工修改防 AI 静默覆盖、最近 4 章全文窗口、旧章压缩索引、最多 30 条相关梦脉检索与命中预览、旧章分支/保留原卷重写/回退/删除后续，以及长梦多文件 ZIP 导入导出。
- 禾禾已在真实 SillyTavern 测试回来，判断长梦整体“感觉上没什么大问题”。仍有几个一般使用体验可优化，但明确安排到最后再说；当前唯一需要优先重新讨论的是梦脉的使用方式和数据结构。
- 禾禾准备与小澄共同修改梦脉方案。当前没有授权继续改梦脉代码；下一步应先接收她们讨论后的理想输出示例、字段清单和提示词草稿，再判断属于提示词调整、提示词预设库，还是需要正式设计梦脉 v2 schema 和迁移。
- 新增未提交讨论稿 `DREAM_MEMORY_DISCUSSION.md`，详细记录当前梦脉定位、真实结构、完整 System 提示词、动态 User 请求、解析/合并/检索规则、用户主权、已知问题，以及提示词预设 JSON 与结构包两级二创方案。该文件只用于讨论，没有改动功能。
- 当前设置本来就允许直接编辑单份梦脉 System 提示词，但不支持多个命名预设、JSON 导入导出或作者信息。建议优先区分两层：
  - **提示词预设 JSON**：只允许二创 System 提示词和公开元数据，程序继续固定注入长梦资料及输出契约；兼容风险低。
  - **梦脉结构包**：允许改变分类、字段、状态机、合并和检索，需要新 schema、白名单校验、旧数据迁移、UI 与备份兼容，不能只靠导入 Prompt 实现。
- 当前真实结构问题已经写入讨论稿，重点包括：`currentState` 与卡片重复且人工修改没有锁定；废止项以后可能被 AI 重新添加；状态原位更新可能丢失重要变化过程；缺少不确定、冲突、已解决、失效与用户否定等状态；`key` 和标签完全依赖模型自由命名；AI 卡片解析成功后直接生效；提示词可以改但字段结构写死。
- 讨论稿中的“理想 v2 JSON”只是帮助禾禾和小澄讨论的例子，不是已确认产品方向。新窗口不得直接据此施工。
- 本轮讨论文档创建后没有运行代码测试，因为没有修改代码；最后一次功能基线仍是提交前通过的 `146/146`。

## 长梦预设、世界书 Token 与诊断修正（2026-08-07）

- 长梦续章不再把所选预设压平成一段“写作风格”。每一轮正文请求都复用普通生成的结构化预设布局，保留启用状态、`prompt_order`、原始 `system/user/assistant`、绝对深度、相邻 system 压合设置与 no-tools 后处理；仍不读取现场聊天、当前角色卡或当前人设。
- 长梦冻结世界书快照新增保留 `depth/role/outletName`，续章时按冻结时的 `position/depth/order/role/outlet` 进入所选预设布局；旧快照缺字段时继续安全回退，不升级插件版本或长梦 schema。
- 世界书页和生成页实时预览现在共用 `estimateTokenCount`，并对同一份“世界书设定”包装文本估算。世界书页文案明确为“已勾选上限约”，表示所有勾选条目的输入上限，不冒充模型官方 tokenizer 或本轮绿灯实际激活量。
- 诊断只保留创作请求的线路、协议、模型、预设、后处理、消息角色/来源、字符数和 Token 预估，不再在内存快照、界面或复制报告中保存消息正文；普通补写、最终 HTML 排版及其他辅助请求不会覆盖正文创作请求结构。无具体来源的回退消息只显示 `system/user`，不再显示 `request/system`、`request/user`。
- 本轮新增 4 项回归；核心测试为 `141/141`。尚需在真实 SillyTavern 中分别用主 API / 独立 API 验证复杂多角色预设、冻结世界书位置，以及复制诊断报告内容。

## 长梦阶段 3 最新施工（2026-08-06）

- 长梦页会记住最后所在标签、长卷详情与尚未提交的章名、方向、目标字数；关闭弹窗或切换插件页后再回来不会回到初始页。后台生成时重新打开会直接回到对应长梦。
- 待确认章节右上角新增小型全屏入口；全屏使用和生成结果一致的安全阅读器。重新生成改为按原章名、原方向和原目标字数一键重跑，不再要求重复输入。
- 酒馆主 API 增强了模型字段兼容；无法识别 `ChatCompletionService` 当前字段时可安全降级到 `TavernHelper`，但内容过滤、429、输出限制不会冒险重复发送。2026-08-07 禾禾确认酒馆主 API 与独立 API 均已实测正常。
- 长梦 schema 升到 4（插件版本仍是 `v3.6.3`）。原世界线关系改为五种明确语义：完全隔离、平行支线/AU、前传补完、原线同期补完、正史后续；非隔离模式冻结当时选中的世界书，续章提示按关系说明资料权威级别。
- “梦脉织录”已经接入：固定使用一个已保存的独立副 API 预设，不占正文 API；默认每累计 3 个已确认章节后台批量提取。章节保存不等待梦脉，失败只保留待重试状态；刷新打断时会从“织录中”恢复成待织录，不会永久卡住。
- 梦脉只读取正式确认章节，输出结构化卡片和当前状态；补丁只追加/去重，不会因一次总结静默删除旧记忆。阶段 3 当时只开放查看和“立即织录”；`6190a6b` 已进一步开放卡片编辑、废止、恢复与当前脉象校正。
- API 设置区改为滑动分段式“独立 API / 酒馆主 API”，并增加独立的“梦脉织录”折叠配置；支持选择副 API 预设、1/3/5 章批量和编辑/恢复内置织录预设。手机端已做单列样式，但仍需真实设备触摸验收。
- 修复“跟随角色卡”跨角色残留：持久化记录当前角色卡自动带入的世界书，切卡时只撤下上一张卡的自动组，再加入当前卡绑定组；用户额外手选的书继续保留。旧版首次升级无法分辨历史手选/自动，因此在跟随已开启时会优先清理一次旧勾选，阻止跨角色串设定。
- 长梦 JSON 备份已同步保留世界线关系和梦脉处理状态；`6190a6b` 又增加大体量多文件 ZIP。两种备份都不包含 API Key 或其他密钥。
- 已新增插件自身的思考标签过滤：主 API/独立 API、流式/非流式、普通正文、长梦正文、最终 HTML 和梦脉统一隐藏 `<thinking>…</thinking>`，并兼容 `<think>…</think>`。思考内容不会进入章节、梦脉、历史、备份或日志；只有思考、没有正文时返回 `T-API-REASONING-ONLY`，且不会换线路重复发送。已包含在 `51a7293`。
- 长梦正文已接入目标字数自动补写：复用设置页现有“字数不足时自动补写”和最大轮数，按可读正文达到目标 `90%` 结束，未达到则逐轮只请求新增正文，达到最大轮数后停止；全部轮次只做一次最终 HTML 排版。每轮流式正文都会写入同一份 `WRITING` 草稿，第二轮或后续轮中途停止时保留此前完整轮次与当前已返回片段。已包含在 `51a7293`。
- 长梦待确认章节已支持最多三版候选：第一版完成后可按同一章名、方向和目标字数继续生成第二、三版，使用 `1/N` 切换预览，只把当前选中版确认晋升为正式章节。生成新候选不会覆盖旧候选；新一版中途停止时旧候选仍保留，放弃本轮可回到待确认状态。候选不进入梦脉，也不随新导出的长梦 JSON 保存；旧备份中的单版待确认稿仍可兼容导入。已包含在 `51a7293`。
- 阶段 3 收尾新增“等待重新排版”恢复态：正文已完成、但最终 HTML 排版失败或在排版时停止后，正文与恢复阶段都会持久化；再次点击只重试最终排版，不会再请求正文或重复追加。该状态随未完成草稿的长梦 JSON 备份保留；新增连续确认五章、防重复追加与排版重试回归。
- 修复普通生成与长梦共用安全 iframe 的首次误判：内嵌预览尚未显示或复杂模板仍在初始化时，`body.innerText` 可能短暂为 0，旧逻辑会过早切成纯文字，而全屏重载后 HTML 又正常。现在会读取剔除脚本/样式后的正文、进行多次延迟上报，并给空正文短暂确认期；没有尺寸回报本身不再让普通生成隐藏丰富 HTML，真正持续为空时仍保留纯文字兜底。

## 长梦阶段 4：AI 定梦建议，但保留用户主权（2026-08-07）

- 定梦创建页新增可选的“AI 帮我整理”入口。请求只携带当前所选第一章的标题与纯正文，不读取聊天前文、现场世界书或未选择的资料；沿用当前界面明确选择的酒馆主 API / 独立 API，使用非流式 JSON 请求。
- 新增纯函数模块 `long-dream-canon-suggestions.js`：把建议限定为人物关系、时间地点、已发生事件、不可违反事实四类；统一过滤 `<thinking>/<think>`，解析与去重 JSON，并把 AI 无法唯一判断的项目保留为“不确定 · 需要确认”。
- 建议只存在于当前定梦页的内存草稿，不写入长梦数据、设置、历史或备份。每条可修改分类和内容、删除、采纳或撤回；重新整理前会提示将替换现有建议状态，但不会改动手写“此梦设定”。切换来源、返回长卷或关闭弹窗会中止并清空建议。
- 点击“确认定梦并开卷”时，只把用户明确采纳且仍非空的建议与手写设定合并；未采纳、已删除和尚在请求中的内容都不能进入 `canon`。用户完全不使用 AI 时，原有手写定梦与开卷路径不变，长梦 schema 也没有因此升级。
- 建议请求与普通生成、长梦续章和自动模式互斥，避免主 API 并发串线；诊断摘要明确显示本轮只读第一章，运行日志只记录来源类型、字数、线路和错误信号，不记录正文或密钥。
- 界面沿用夜色档案 / 纸页批注风格：采纳项与待决定项有明确状态，不确定项使用独立警示标记；375px 窄屏下分类、按钮和摘要改为单列。尚需在真实 SillyTavern 验收两类 API、停止/切源竞态、三种皮肤和触摸操作。
- 新增 4 项回归，覆盖请求只读第一章、JSON/不确定项解析、只有采纳项进入 canon，以及定梦 UI 的编辑/删除/采纳入口；核心测试现为 `137/137`。

## 长梦补全四阶段：阶段 1 请求保真（2026-08-06）

- 新增 `request-layout.js`，把酒馆预设按启用状态、`prompt_order` 与原始 `system/user/assistant` 角色保留为消息序列；空内容的标准动态锚点不再被提前丢弃。普通小剧场首轮已使用这份结构化序列，自动补写与最终排版仍使用各自明确的简化请求。
- 世界书条目不再只剩内容字符串：保留 `position/depth/order/role/outletName`，兼容酒馆 0–7 八类位置（角色定义前后、作者注释上下、指定深度、示例消息上下、Outlet）。指定深度按聊天消息与角色插入，Outlet 只在预设显式出现 `{{outlet::名称}}` 时展开；蓝绿灯触发仍复用酒馆原生扫描。
- 预设后处理兼容 none/merge/semi/strict/single，并把 `merge_tools`、`semi_tools`、`strict_tools` 强制转换成不带工具的同类模式；请求归一化会删除 tool/function 消息及 tool call 字段。主 API、TavernHelper 和独立 OpenAI/Anthropic 请求均不发送 `tools` 或 `tool_choice`。
- 新增 `request-trace.js` 与诊断页“实际请求消息”折叠区：快照在最终消息进入传输层前生成，仅保存在内存，显示线路、协议、模型、预设、后处理、来源与完整消息顺序；不记录 API Key、Authorization 或独立 API URL，复制诊断也使用脱敏内容。
- 独立 API 继续只使用函数入参中的显式配置，不读取或改写酒馆主线路；新增两个独立连接并发时地址、模型、Key 与消息互不串线的回归测试。
- 新增纯函数与传输层回归测试后，`node --test --test-isolation=none .\tests\core.test.js` 为 `128/128`；`index.js`、`api-client.js`、`api-runtime.js`、`request-layout.js`、`request-trace.js` 语法检查通过。尚未在真实 SillyTavern 验证复杂预设与八位置世界书的实际表现。
- 禾禾提供真实复杂预设 `【MoM】果实G5.50丨喵喵电波@KKM@YUKI-2.json` 后明确了产品范围：插件不复制酒馆编辑器，只读取酒馆已有分类和深度。当前修正读取并使用预设的 `role/injection_position/injection_depth/injection_order/squash_system_messages`，绝对深度条目按角色插入聊天历史，连续 system 按预设设置合并而 assistant/user 断开；预设列表显示角色与 `@深度`。世界书继续读取 `position/depth/role/order`，列表显示位置或 `@D深度 · 角色`，深度不在插件内擅自限制为 100。新增回归后为 `130/130`，该修正已包含在 `81895a1`。

真实验收仍未全部完成：禾禾与朋友已经确认角色卡自动世界书替换正常，禾禾也已确认酒馆主 API 与独立 API 均可正常生成。新增的排版恢复态、首次内嵌 HTML 不误降级、真实流式停止/恢复、完卷/恢复、导入导出和手机端仍需逐项人工复测。

## 设置页内部重排（2026-08-05）

- 禾禾确认采用“只调整设置标签内部”的修正版原型；现有千夜浮梦标题、九个顶部标签、其他功能页和弹窗整体结构均未改动。
- 设置内部仍保留 API 与输出、生成规则、提示与操作、扩展管理四组，但删除桌面左侧的第二套设置目录，改为单列折叠清单。
- API 预设的新建/更新/改名/删除收进 `⋯`；模型获取缩到模型输入框尾部；测试连接与应用更改集中到 API 区底部。所有原有功能 ID 与事件入口保留。
- 梦脉织录仍在 API 组内按需展开，摘要直接显示副 API 和批量章数；提示音与自动模式的低频参数也改为折叠详情。更新、刷新和复制日志改为紧凑行内操作。
- 样式继续使用插件现有主题变量，适配默认、跟随酒馆与自定义皮肤；手机端改为单列字段和紧凑操作区。
- 当前相关 JavaScript 语法检查、CSS 花括号检查通过；核心测试 `114/114` 通过。尚未在真实 SillyTavern 中完成视觉与触摸验收。

## 最新界面施工（2026-08-04）

- 生成结果标题改为居中独占一行，最近生成分页另起一行；手机端隐藏分页时间，只保留页码。
- 原先常驻的六个结果按钮改为轻纸质“页边书签”：随弹窗页边固定，可上下拖动、松手吸附左右并持久化位置；点击后向屏幕内侧展开完整操作菜单。书签可在设置中关闭，关闭后分页右侧仍保留“···”入口，不丢功能。
- 设置页没有删减原功能：通过桌面左侧分类索引 + 四组折叠卡片重排为“API 与输出 / 生成规则 / 提示与操作 / 扩展管理”；手机端隐藏索引并使用单列折叠卡。提示音、自动模式详情、API 预设增删改、模型获取/测试、刷新酒馆确认等入口全部保留。
- “常见问题汇总”已从生成后的诊断报告内部移出，成为诊断页中位于“插件诊断”后方的独立折叠界面；无需先生成报告就能查询。复制诊断报告只复制本次状态，不附带整份问题词典。
- `result-bookmark.js` 封装书签位置归一化、边缘吸附和可见范围计算；新增静态与纯函数回归测试。
- 长梦首页“导入备份 / 导出全部”在默认深色档案卡中使用深色半透明底与暖白文字，不再被全局白色按钮规则覆盖；禁用态仍保持可辨识。
- 常见问题卡改为纵向信息层级：第一行“错误信号＋问题标题”，第二段“原因”，最后保留“可以怎么处理”和同类信号，不再在手机上挤成竖排。
- 页边书签展开菜单改为实体背景：默认皮肤为不透明暖米白，跟随酒馆皮肤使用主题实底，自定义皮肤提供可覆盖的 `--theater-result-menu-bg` 兜底。
- 手机内容区重新按外层 `92dvh`、Close 控件和安全区计算高度，补入底部滚动留白；标签切换回到顶部，桌面设置分类导航只滚动插件内部容器，不再调用全页面 `scrollIntoView`。
- 当前没有升级版本；真实 SillyTavern 仍需手动验收手机能否完整滑到最后一个控件、两种长梦备份按钮状态，以及三种皮肤下的操作菜单底色。

## 已定稿的核心产品逻辑

### 1. 字数目标与请求轮次

- 单轮输出上限默认保留 `16384 Token`；若模型不接受该上限，按标准档位自动降低并重试。
- 模型首轮必须明确知道目标正文字数，但不要求模型自报或统计字数。
- 程序统计“可读正文”字数，排除 HTML、CSS、JavaScript、注释和排版代码。
- 用户指令里有明确字数时自动识别；“独立设置目标字数”默认关闭，开启后覆盖指令中的字数。该设置面板可以折叠。
- 没有明确目标字数时只生成一轮，不触发自动补写。
- 自动补写默认开启，默认最多 3 轮，可在设置中调整为 1–10 轮。
- 达到目标的 `90%` 视为完成。
- 若动态收束轮已经获准收尾，模型正常完成结局但仍不足 90%，任务直接结束，不再强行续写；界面显示“已完成，约 X 字，低于目标 X 字”。若该轮是被 Token 截断，则仍可在轮数范围内继续。

当前实际分段策略：

- `< 5000 字`：保持原方式，正文和 HTML 可在同一生成流程中完成。
- `5000–7999 字`：先生成完整纯正文，再单独请求最终 HTML 排版。
- `>= 8000 字`：开启自动补写且允许至少两轮时，正文固定最多两轮；上半篇纯正文停在剧情中段，下半篇承接并完成，最后再独立请求 HTML 排版。若用户关闭自动补写，则不会强行拆成上下篇。

注意：代码中的写作舒适度分类边界与渲染分段边界不是同一件事。`length-policy.js` 中的 `SHORT / COMFORT / LONG` 用来控制文案和节奏；真正决定“正文与 HTML 分离”的阈值是 `5000`，决定“上下篇”的阈值是 `8000`。

### 2. 自动补写与手动续写

- 自动补写请求只携带上一段结尾原文和当前字数计划，不携带完整 A+B，不携带原始长指令，不重新打开 HTML 结构。
- 手动点击“续写”时，只从当前显示结果提取纯正文；绝不把 HTML、CSS 或脚本带进下一次请求。
- 连续手动续写只携带上一轮正文，不累计更早轮次；前情最多保留最近 `8000` 字，过长时截取后半段。
- 续写模型只应输出新增正文，不复述前文。
- v3.5.2 的关键修复是：续写生成期间即使实时区展示的是流式正文，最终结果仍应保留并恢复为 HTML，而不是被纯文字覆盖。
- v3.6.2 起，续写前情会先清理 HTML 排版产生的多余空格和换行；界面、诊断与 8000 字截取统一按可读正文统计。
- 用户本轮指令现在位于前情之后、最终渲染与创作规则之前，对齐酒馆“当前消息后仍有最终指令”的顺序。

### 3. 最终 HTML 排版

- 长篇先写正文再排版，不能以“稳定”为理由长期降级成普通纯文字或简陋 HTML。
- 最终排版请求使用段落占位方案，要求每个正文段落恰好出现一次、顺序不变、位于可见文本节点中。
- 程序检查排版后字数，避免漏段、乱序、隐藏正文、重复正文或模型擅自改写。
- 最终 HTML 校验失败会自动再试一次；仍失败时保留已经生成的正文，不能丢稿。

### 4. 世界书

- 世界书蓝灯/绿灯模式直接复用 SillyTavern 原生扫描逻辑，而不是插件自己仿造关键词判断。
- 蓝灯：常驻触发；绿灯：关键词命中后触发；链式/其他策略由酒馆规则处理。
- 插件只把用户勾选的候选条目交给酒馆扫描，再接回本轮真正触发的结果。
- 酒馆扫描不可用时有回退路径，并写入脱敏运行日志。

### 5. 上下文、模板与移动端

- 聊天前文楼层数改为数字输入，允许 `0–500`；填 0 表示完全不读取聊天消息。
- 指令模板的 JSON 导出/导入会保留文件夹分组和空文件夹；兼容旧 TXT 批量导入及 Windows 换行。
- 世界书和模板列表的手机布局已经压缩重排；顶部重复的“全选 / 全不选”已移除。
- 魔法棒菜单入口已固定排序，避免每次刷新在其他扩展之间忽上忽下。

### 6. API、空流、429 与日志

- 支持酒馆主 API 和独立 API；独立 API 支持自动/OpenAI/Anthropic 格式及流式/非流式兼容。
- 不要把 429 文案写死成“公益站限流”。统一说明为请求过于频繁或当前线路繁忙，并按响应里的 `Retry-After` 等信息处理重试。
- `Stream empty` 会尝试识别流内错误、空流和可恢复情况；若后续轮出错，必须保留此前生成的正文。
- 运行时弹窗只显示短错误信号（例如 `T-HTTP-429`、`T-HTTP-500`、`T-API-CONTENT-FILTER`）；诊断页在“插件诊断”后提供独立、默认收起的“常见问题汇总”，可按信号查询常见原因、插件已做处理和可自行调整的方向，不混入单次报告正文。
- `content_filter` 只说明上游针对组合请求没有交付正文，不等于本轮用户指令一定含 NSFW；明确的内容策略结束不会再被误判为空流并使用同一提示词重发。HTTP 状态体和上游回显不进入弹窗、诊断或可复制运行日志。
- 自动模式达到楼层间隔但来源没有可用正文时，会给出 `T-AUTO-NO-INSTRUCTION` 并写一次脱敏日志，不再静默跳过；用户补齐来源后无需重新等待完整间隔。
- 运行日志只保存在内存最近 200 条；复制前统一隐藏 API Key、Authorization、敏感 URL 路径等信息。
- “复制日志”已加入多级剪贴板兜底与人工复制弹窗，不能在没有真正写入剪贴板时只提示“已复制”。
- 插件更新成功后不会自动刷新酒馆；设置页会出现“刷新酒馆并启用”按钮，点击后必须再次确认，取消则继续停留在当前页面。若仍有生成任务，确认文案会明确提醒刷新将中断任务。
- 所有新增日志都按“可能被用户直接公开转发”的标准处理，禁止记录或回显密钥。

### 7. 阅读模式

- 纯文字模板分为亮色与暗色夜读，两者都只要求模型输出正文，配色由插件本地完成。
- 生成区支持全屏阅读，可显示 HTML 小剧场及两种纯文字主题，并支持按钮或 Esc 退出。
- 全屏阅读使用浏览器顶层弹窗，不会再被酒馆的插件窗口压在背后。
- HTML 无法正常显示时会保留正文并切换到安全的纯文字展示，避免生成完成后只剩空白。

### 8. 长梦续章（阶段 4–8 功能补全，本地待验收）

当前产品决定：长梦续章是独立于普通生成页和普通“续写”的长篇支线入口，不把更多控件继续塞进已经拥挤的生成界面。

长梦测试分支当前状态：

- 顶部新增独立“长梦”入口，包含长卷列表、开启长梦的“定梦”流程和长卷详情页；视觉采用夜色档案 / 纸页长卷方向，并有手机窄屏适配。
- 可以从当前正在查看、最近生成或历史记录中选择一篇作为第一章；首章同时保存完整 HTML、可读正文、原指令、输出模式和来源快照，不会牺牲已有 HTML 丰富度。
- 新增长梦数据模块 `long-dream.js`，维护长卷、章节、世界线定义、来源配置快照和“梦脉”容器。
- IndexedDB 从版本 1 升到版本 2，新增独立 `dreams` 仓库；长梦不会和普通历史混存。IndexedDB 不可用时仍回退到 settings。
- “定梦”由用户明确确认，不依赖 API 猜关系：默认“完全隔离”，也可选择平行支线/AU、前传、原线同期或正史后续；非隔离模式冻结当时选中的世界书。
- 详情页可以修改长卷名字、此梦设定和世界书继承策略；修改定义不会改写或丢失已经保存的章节。
- 长卷详情提供折叠章节目录；任意旧章都走独立的安全沉浸阅读器，不会覆写普通生成结果、最近生成或普通历史。每章可安全另开支线；第二章起可在保留原卷的前提下重写本章，也可明确确认后回退到某章或删除某章及后续。会改变时间线的操作都会清掉失效梦脉并重新织录；普通历史不受影响。
- 详情页已按真实验收反馈简化为单一主流程：“填写本章方向 → 续写下一章”。最新前文缩成次级卡片；章名和目标字数收进“更多选项”；名字、此梦设定、世界线关系和删除收进折叠的“长梦设置”；梦脉以紧凑状态条和可折叠卡片列表呈现。
- 已确认旧历史存在来源元数据边界：早期记录可能没有 `instruction`；旧版“保存历史”即使写入了指令，也可能取自保存当下的输入框，无法保证与正文一一对应。创建长梦时现会区分“已绑定保存”“旧记录请核对”“未保存指令”，不再拿当前输入框冒充旧作规则。以后新生成结果会把指令、预设名、世界书名、上下文范围和渲染档案作为非敏感 `sourceConfig` 与正文绑定，并由历史 ZIP/JSON v2 保留；旧数据无法凭空恢复。
- `long-dream.js` 的 schema 已升到 4，补入追加/更新章节、从指定章节截断后续、完卷状态、草稿保存/清理/确认晋升、世界线关系、梦脉状态、目标字数恢复和旧记录迁移。正式章节必须同时具备纯正文与最终 HTML；草稿不会被当成正式章节。
- 明确沿用世界书时会冻结实际条目内容和非敏感匹配字段；切回默认隔离会清掉继承快照，避免以后误用旧世界线。原世界书后续变化不会静默进入已建立的长梦。
- 新增纯函数模块 `long-dream-payload.js`，只读取长卷自身的定梦、章节、已确认梦脉与允许的冻结快照，不读取聊天前文、普通续写缓存或现场世界书；续章只带最近 4 章全文，较早章节使用有 12000 字符硬上限的首尾索引，并按本章方向从有效梦脉中最多检索 30 条。界面会实时预览本章命中的梦脉；定梦和本章方向不会先于低优先级信息被裁掉。
- 新增独立生成控制器 `long-dream-generation.js`：长梦拥有自己的停止信号和运行状态，不复用普通生成的 `continueContext`、最近生成或任务状态；流式正文会形成可恢复的 `WRITING` 草稿，最终排版成功后只进入 `REVIEW`，显式确认后才把正文与 HTML 原子晋升为正式章节。停止、请求失败或排版失败都不会追加半章；刷新前已有草稿可以继续承接。
- 测试分支已把“续写下一章”接到现有 `api-runtime.js`：主 API/独立 API、SSE/非流式、停止、429 与空流恢复继续共用现有实现；长梦默认不读取聊天前文、普通续写缓存或现场世界书。最终 HTML 排版的占位、完整性校验和单次修复重试已经抽成普通生成与长梦共用函数。
- 续章时按钮会切换为“停止”；完成后在“梦中”页显示安全 iframe 预览，并停在待确认状态。只有用户点击“确认保存”才追加正式章节；也可以放弃重写。普通生成和自动生成在长梦请求期间不会并发启动。
- 已接入非破坏性卷状态：无生成和草稿时可“确认完卷”，完卷后保留所有章节、停止续写；可随时“继续此梦”恢复，不会改写旧章。
- 已接入长梦专用 JSON 与多文件 ZIP 备份：单卷不超过 25 MB 时仍可导出便携 JSON，导出全部或单卷超过 25 MB 时自动使用 ZIP；ZIP 以清单保存定梦、继承策略、章节元数据和梦脉，每章正文/HTML 分文件保存，压缩包与解压内容均有 512 MB、5000 文件安全上限。导入永远新建副本，不覆盖现有长卷，也不携带待确认候选、本地 ID、未知字段或 Key；只应导入可信来源。

当前完成状态与剩余验收：

- 梦脉已按人物状态、人生经历、关系、世界线偏离、伏笔/约定、地点/物品等维护“当前有效状态”：`type + key` 相同的状态原位更新，重复事实合并来源章；人工修改会被标记并阻止后续 AI 静默覆盖，可废止和恢复，当前脉象也可人工校正。
- 梦脉提示已要求世界线偏离依次记录“原线事实 → 本梦改变 → 直接结果 → 此后不能再默认成立的关系/经历”；没有原线资料时必须标明未知，不得猜测。
- 几十章后的相关性检索、旧章压缩与命中预览已接入；请求体不再随全部章节全文和全部梦脉无限增长。
- 章节管理已公开安全按钮：另开支线始终保留原卷；重写本章会从前一章建立新卷并预填旧方向；回退/删除会明确显示影响范围并二次确认，随后重织梦脉。
- 多文件 ZIP 已接入长梦专用备份；普通历史 ZIP/JSON 仍不包含独立 `dreams` 仓库，应使用长梦页的导入导出。
- 正式开放前的请求保真代码已完成：API 线路隔离、固定不调用工具、实际发送内容诊断、酒馆预设多消息角色/顺序与世界书位置均已有回归测试；仍需真实酒馆验收复杂预设、世界书八位置和不同线路。
- 真实酒馆完整验收：代码已具备主/独立 API、流式、停止与草稿、最终 HTML、待确认、确认保存、完卷恢复、JSON/ZIP、梦脉校正与分支闭环；主/独立 API 与角色卡世界书替换已经通过，新增命中预览、章节分支/回退、ZIP 大备份、排版恢复、停止/恢复、完卷和手机端仍需逐项人工复测。
- 正式发布与版本号更新；当前代码常量、manifest、样式头仍保持 `v3.6.3`。本轮只推送测试分支，不视为正式发布。

本轮验收边界（更新至 2026-08-08）：

- 已用实际长梦 DOM/CSS 做过桌面与 375px 窄屏静态预览：长卷卡片整卡可点击，键盘焦点可见，返回/阅读/主操作按钮最小高度 44px；续章表单在窄屏会变为单列，未发现横向溢出。
- 禾禾已在真实 SillyTavern 安装测试分支并看到“长梦”；第一轮按钮、创建、保存、阅读和删除暂未发现故障。用不同历史再次开卷时发现较早记录的规则为空或与正文不可靠对应，由此触发上述来源元数据修正。第二轮流程简化、来源提示和实际 API 接线均已进入测试分支。
- 禾禾已在真实 SillyTavern 确认独立 API 与酒馆主 API 均正常，并与朋友确认角色卡切换时自动世界书会正确替换。流式停止/草稿恢复、最终 HTML 首次内嵌预览、待确认与保存、完卷/恢复、导入导出仍不能描述为整套人工验收完成。
- 测试分支已补齐章节目录、独立阅读、完卷/恢复、白名单 JSON/ZIP 导入导出、梦脉织录与人工校正、近期全文窗口与旧章检索、命中预览、世界线关系、章节分支/回退/删除、请求错误信号与诊断对照、待确认章节三版候选、阶段 1 请求保真、`<thinking>` 自动隐藏、排版失败只重试排版和 iframe 首次误降级保护；`node --test --test-isolation=none .\tests\core.test.js` 当前为 `146/146` 通过。

下一阶段的重要边界：

- API 不负责“智能分清原世界线与 AU 世界线”。程序必须只按用户确认的 `canon` 和继承策略组装请求；AI 可以提出设定草稿，但不能静默决定人物关系。
- 默认分支隔离时，后续请求不得重新扫描或注入原世界书；只有用户明确选择沿用时，才使用长卷内已经冻结的世界书内容快照，不能只凭书名现场重读。
- 长梦续写要消费整部长卷自己的章节与梦脉，不能复用普通续写“只带上一轮 8000 字”的语义，也不能改变普通续写的既有行为。

#### 8.1 产品定位与明确不做的事

长梦续章解决的是“同一个小剧场连续续写很多次以后，人物关系、剧情因果、伏笔和细节逐渐混乱”的问题。它不是把整个 SillyTavern 重新做进插件，也不是“小手机 2.0”。

- 普通小剧场继续负责一次性生成和短续写；长梦只服务于已经决定长期连载的某一场小剧场。
- 长梦使用独立入口，是因为生成页的基本信息已经接近视觉容量上限。长梦的章节、记忆和世界线控件不得重新塞回普通生成页。
- 普通“续写”仍保持点击后自动回到生成页顶部并聚焦指令框；不要为了长梦改坏这条短续写路径。
- 不在长梦里复刻酒馆全部预设插入位、严格工具协议、角色前/角色后、chat history 排序或完整世界书引擎。确有价值的兼容问题可以单独修，但不能让小剧场扩张成微型酒馆。
- 不整包合并他人的大改版；其中确有价值的问题应逐项验证、独立实现。对方若希望保留完整的微型酒馆路线，可以自行维护二改分支，不要求主版全部吸收。
- 不引用或搬运尚未取得作者授权的“小手机”代码。可以独立讨论“古法总结”等通用思路，但实现必须由本项目自行设计。
- 不读取或修改酒馆里未由用户选择的 API 配置，不偷偷切换正文使用的 Key，也不在长梦记录、日志或备份中保存 API Key。长梦只通过插件现有、界面上可见的 API 路由发请求。
- 不把原聊天几千楼的长期记忆系统原样搬来。长梦的记忆单位是“章节”，不是酒馆楼层；应针对小剧场连载设计更轻、更透明的方案。

#### 8.2 核心概念和数据职责

- **长卷（dream）**：一条独立连载世界线，包含名字、状态、创建时间、此梦设定、世界书继承策略、章节和梦脉。
- **章节（chapter）**：每次确认保存的一次续写结果。必须同时保存纯正文与最终 HTML；纯正文供上下文和检索，HTML 供原样阅读与导出。
- **定梦（canon）**：由用户确认的这条支线的硬事实，例如“没有血缘关系，是青梅竹马”。它不是普通写作指令，也不应由 AI 静默修改。
- **梦脉（memory cards）**：从章节中提取的结构化剧情记忆，用来在章节很多以后找回人物状态、事件、伏笔和关键原话；它不能替代章节原文。
- **来源快照（source/sourceConfig）**：记录第一章来自当前结果、最近生成还是历史，以及当时使用的模式、指令、预设名称等。只记录复现所需的非敏感信息，不保存 Key。
- **继承快照（future world-book snapshot）**：若用户明确选择沿用世界书，正式接入续章前应冻结当时允许继承的内容或版本摘要，而不只保存书名。此后原世界书变化不应悄悄改写已经成立的长梦；刷新继承必须由用户主动确认。

建议后续为每张梦脉保留这些字段：

- `type`：事件、人物状态、关系变化、地点/物品、伏笔、约定、关键原话等。
- `content`：短而明确的事实，不写空泛文学总结。
- `chapterId` / `chapterNumber`：可以回到原章节核对。
- `quote`：确有价值时保留很短的原句或对话片段，避免古法总结丢掉细节口吻。
- `status`：待确认、已确认、已废止；AI 新提取的内容不能天然等同于用户确认的定梦。
- `tags`：人物、地点、物品或伏笔关键词，为后期检索服务。

#### 8.3 世界线隔离与请求优先级

API 本身不会可靠地知道“原世界书中的兄妹”和“长梦中的青梅竹马”哪一个才是本次事实。可靠性来自程序确定性地分层、裁剪和标注上下文，而不是期待模型自行领悟。

后续续章请求应按以下优先级组装：

1. 用户本次明确写下的章节方向，但不能在无提示的情况下推翻已经确认的定梦。
2. 用户确认的 `canon`，作为最高级的持续世界线事实。
3. 长卷已经保存的章节事实和已确认梦脉。
4. 用户主动允许的冻结世界书快照；默认分支隔离时这一层完全不存在。
5. 写作预设、风格附加规则和渲染规则；这些只控制表达，不得改写事实。

冲突处理原则：

- `canon` 与原世界书冲突：以 `canon` 为准，并且不要把冲突的原条目发给模型。
- 新章节方向与旧定梦冲突：界面提示用户选择“修改定梦”或“保留旧设定”，不能由模型暗中圆场。
- 已保存章节彼此出现矛盾：先标为待处理冲突，不自动删除任何章节或梦脉。
- 用户从“只沿用此梦世界线”切换成“沿用世界书”时，需要明确显示准备加入哪些内容；反向切换时停止后续注入，但不改写过去章节。

#### 8.4 梦脉为何不能只做一段古法总结

把第 1–300 条内容总结成一段话再勾选注入，优点是简单、省 Token；缺点是会压掉细节对白、物件去向、人物细微态度和未兑现伏笔。对长梦而言，只有一段滚动摘要不够。

暂定采用“原文章节 + 结构化梦脉 + 近期窗口”的混合方案：

- 章节不多时，直接携带全部章节纯正文，不急着总结，优先保证细节完整。
- 每确认保存一章后加入待织录队列；默认累计三章后用副 API 批量提取，用户也可手动立即织录。卡片可人工修改、废止和恢复，人工校正后的状态槽不会再被 AI 静默覆盖。
- 章节增长后固定保留最近 4 章全文；更早章节提供有硬上限的压缩索引，并按人物、地点、物品、标签和本章方向检索相关梦脉。
- 单次检索最多 30 条有效梦脉；本章方向输入框下方实时展示命中数量和条目，诊断报告只保留命中数量，不复制梦脉正文。
- 对关键对白、约定、伏笔原句，梦脉保存短引用和章节定位；需要更多细节时仍能回看原章，不能只剩摘要。
- 梦脉提取失败不应阻止章节保存；记忆是增强层，章节原文才是不可丢的底稿。

#### 8.5 目标用户流程

1. 用户在普通生成区得到一篇满意的小剧场，或从历史里打开一篇旧作。
2. 进入独立“长梦”页，选择这篇作品作为第一章。
3. 在“定梦”中确认长卷名、支线硬设定以及是否沿用选中的世界书；默认隔离原世界书。
4. 进入长卷后，主区域直接显示“续写第 N 章”和本章方向；用户不需要先穿过前文、梦脉或设置表单。
5. 章名与目标字数属于“更多选项”；最新章节是次级阅读卡片；名字、此梦设定、继承策略和删除位于折叠的“长梦设置”。页面以简短状态条说明已保存章节、梦脉和继承范围。
6. 模型只返回新增章节正文；程序完成现有安全排版后，先让用户阅读，再原子性保存正文与 HTML 为新章节。
7. 保存成功后异步排队，默认每三章批量织录梦脉；不强迫每章都打断阅读流程。
8. 用户可在章节目录中阅读旧章、重写某章、从某章另开分支或回退；这些操作必须有明确影响范围，不能静默覆盖后续章节。
9. 完卷后可以导出整部长梦；再次导入时恢复定梦、章节、梦脉、继承快照和原始 HTML，但不包含任何密钥。

#### 8.6 详细开发顺序与每阶段完成标准

**阶段 0：验收第一阶段骨架**

- 在真实 SillyTavern 中检查顶部入口、空状态、定梦、长卷列表、详情页和“阅读本章”。
- 分别检查默认皮肤、酒馆皮肤、自定义皮肤，以及手机窄屏、非全屏和全屏阅读。
- 用当前结果、最近生成、历史记录各开一卷，确认不会错带上一轮正文或指令。
- 完成标准：禾禾确认信息层级和操作路径自然，普通生成、历史和普通续写没有回归。

**阶段 1：稳固章节数据层**

- 在 `long-dream.js` 中加入追加章节、更新章节、删除/截断后续章节、完卷状态和数据迁移函数。
- 保存新章节时使用完整对象校验；正文和 HTML 必须同一次事务成功，失败时保留旧长卷。
- 设计“生成中的草稿”恢复，但草稿不能冒充已经确认的正式章节。
- 为明确沿用世界书的长卷增加非敏感继承快照或版本摘要。
- 完成标准：刷新页面、数据库回退、异常中断后都不会丢已保存章节，也不会产生半章。

**阶段 2：建立长梦专用上下文组装器**

- 单独建立类似 `long-dream-payload.js` 的纯函数模块，不把长梦条件继续堆进普通 `generation-payload.js`。
- 输入只接受当前长卷、用户本章方向、明确允许的世界书快照、当前插件 API/预设选择和目标字数。
- 输出明确分区：此梦设定、允许继承的世界书、梦脉、历史章节、当前章方向和输出协议。
- 默认隔离时不得调用原世界书扫描；不得读取酒馆聊天前文；不得带入普通续写缓存。
- 添加冲突和 Token 预算测试，保证定梦永远不会在裁剪时先于低优先级信息被删掉。
- 完成标准：通过单元测试可以证明，同一份原世界书在“兄妹原作 / 青梅竹马长梦”场景下不会进入错误请求。

**阶段 3：接入“续写下一章”生成闭环**

- 在长卷详情中提供独立续章面板：章名、目标字数、本章方向、将读取的章节/梦脉/世界书状态。
- 复用已经稳定的 API 客户端、流式显示、停止、429/空流恢复、字数统计和最终 HTML 排版；不复制第二套网络实现。
- 网络运行能力已在阶段 3 前提取到 `api-runtime.js`；接线时由长梦传入自己的 payload、停止信号和流式回调，普通生成与长梦共用该模块。
- 请求语义是“新增一章”，不是普通短续写；不得把旧 HTML/CSS 发给模型，也不得重复旧章节。
- 生成失败或用户停止时先保留可恢复草稿；只有正文与最终显示结果有效后才追加正式章节。
- 新章只进入当前长卷，不自动塞进普通历史或最近三次生成；若用户想另存普通历史，应显式操作。
- 完成标准：连续生成至少 5 章，人物关系、章节顺序和 HTML 阅读均正确，失败重试不会重复追加。

**阶段 4：加入 AI 定梦建议，但保留用户主权**

- AI 可以根据第一章提出“人物关系、时间地点、已发生事件、不可违反事实”的设定草稿。
- 草稿逐项展示，用户可以修改、删除、接受；未确认内容不能写入 `canon`。
- AI 不确定时应标注“不确定/需要确认”，不使用看似聪明的自动归类掩盖歧义。
- 完成标准：不开启 AI 建议也能正常开卷；接受建议前后有清晰差异，且永远不会静默覆盖用户手写定梦。

**阶段 5：接入梦脉提取和校正（已完成，待真实酒馆验收）**

- 已实现确认章节进入异步队列，默认每三章批量提取；类型覆盖事件、人物状态、关系变化、伏笔、物品/地点、约定、关键原话和世界线偏离。
- 梦脉可编辑、废止和恢复；AI 状态更新按稳定槽位原位合并，人工修改拥有更高优先级，不强迫用户每章立刻整理。
- 已实现提取请求只使用尚未处理的确认章节、已有梦脉和有上限的原世界书参考，不重复发送已处理旧章。
- 已实现失败时写脱敏诊断并允许稍后重试，不影响章节阅读或下一章生成；刷新中断也会恢复为待织录。
- 完成标准：梦脉能追溯到原章；错误记忆可以撤销；旧梦脉不会因一次新提取被无声覆盖。

**阶段 6：处理真正的长连载记忆**

- 已设定最近 4 章全文窗口；窗口外按本章方向检索最相关的有效梦脉，最多 30 条；旧章索引固定不超过 12000 字符。
- 检索会提高人工修改、人物状态、关系、世界线偏离、伏笔/约定、地点/物品与近期来源的优先级；普通无关事实优先降级。
- 已增加本章梦脉命中预览；诊断中只显示命中数/有效总数，不复制真实梦脉内容。
- 对几百章场景仍需真实数据复测索引质量；当前不引入复杂向量库。
- 完成标准：构造几十章测试时，早期关键事件可被正确召回，同时请求体不会无上限增长。

**阶段 7：章节管理与分支操作**

- 已完成章节目录、任意旧章独立阅读、长卷重命名、完卷/恢复、从任意章结尾另开支线、保留原卷重写中间章、回退到指定章，以及删除指定章和其后章节。
- 重写始终创建新支线并保留原卷；回退和删除会提示准确影响范围，建议先另开支线，不会直接覆盖整条时间线。
- 删除和截断需要二次确认；执行后清除失效草稿和梦脉，并把保留章节重新加入织录队列。
- 完成标准：所有会影响后续章节的操作都能在执行前说明范围，且原分支不会意外丢失。

**阶段 8：备份、兼容和发布**

- 已有 v1 长梦专用 JSON：校验格式/版本、白名单化保存、导入新建副本且不覆盖、25 MB 上限；普通历史备份不会被当成长梦导入。
- 已有 v1 长梦专用 ZIP：清单保存定梦、章节元数据、梦脉和继承策略，每章 HTML 与纯正文独立存放；导出全部或单卷 JSON 超过 25 MB 时自动使用 ZIP，并校验压缩包、解压体积和文件数量。
- 做真实移动端、不同皮肤、不同 API 协议、刷新恢复、数据库失败和大体量存档验收。
- 最后才同步更新 `VERSION`、`manifest.json` 和 `style.css` 文件头，整理公告、commit 并在禾禾明确要求后 push。
- 完成标准：备份跨设备可恢复，所有自动测试和人工清单通过，发布包中不含 Key、调试预览或私人数据。

#### 8.7 实施时必须持续检查的风险

- **上下文爆炸**：不能永久把全部章节全文塞进请求；先保证正确，再在真实规模出现后逐步引入窗口和梦脉检索。
- **总结失真**：梦脉只能帮助找回信息，不得覆盖原章节；所有事实应能追溯到来源。
- **世界线回流**：任何对酒馆世界书和聊天前文的读取都必须经过长梦继承策略，默认路径为不读取。
- **普通功能回归**：长梦生成状态、最近生成索引和普通 `continueContext` 必须相互独立。
- **HTML 丢失**：存储、重写、备份和全屏阅读都必须保留完整 HTML，不以纯文字降级换表面稳定。
- **密钥泄漏**：长梦只记录 API 预设名称等非敏感引用；日志、诊断和导出继续执行现有脱敏规则。
- **范围膨胀**：每一项新需求先判断是否直接提高小剧场连载的连贯性、可控性或可恢复性；若只是复刻酒馆原生能力，原则上不进入主版。

### 9. 从 local.8 二改中决定吸收的五项请求还原改进

禾禾已经决定：不整包合并对方的 local.8，也不把插件改成微型酒馆；但下面五个问题确实会影响小剧场请求是否按用户原本的预设与世界书工作，因此可以逐项纳入主版。实现时以对方的差异说明和测试场景作为问题线索，代码仍需独立审查、按本项目结构实现，不能直接覆盖现有主流程。

#### 9.1 保留原生预设的消息顺序、角色和插入锚点

**问题：** 当前插件主要读取预设内容后自行拼接，容易把原本分散在 SYSTEM、USER、ASSISTANT 的多条消息压平，也可能让 `chatHistory`、角色描述、示例对话、作者注释等锚点失去原来的相对位置。依赖 SYSTEM/ASSISTANT 模拟对话的破限预设、角色卡 prompt override 或严格消息顺序时，效果会明显偏离正文。

**主版准备吸收：**

- 按酒馆预设的 `prompt_order` 读取启用项，不再只把所有表层 JSON 内容合并成一条 SYSTEM。
- 保留 SYSTEM／USER／ASSISTANT 角色、空锚点和原有相对顺序。
- 至少正确处理 `main`、`nsfw`、`jailbreak`、`worldInfoBefore/After`、角色描述与性格、场景、用户 persona、示例对话、`chatHistory` 和作者注释。
- 尊重角色卡 prompt override、`forbid_overrides` 以及原生 absolute prompt 的 depth、order、role；具体字段必须以当前酒馆运行时为准，不凭 local.8 的旧版本假设硬编码。
- 小剧场自己的最终创作指令仍放在清晰、可测试的位置，不能被预设锚点吞掉，也不能占用正文生成的真实 USER 楼层。

**不准备照搬：** 不为此复刻酒馆完整预设编辑器，也不要求用户改用 local.8 的全部上下文分支和虚拟 USER 规则。

**完成标准：** 用每个锚点都带唯一标记的测试预设生成消息数组，逐条核对角色、顺序、空锚点和小剧场指令位置；普通简单预设的生成结果不能回归。

#### 9.2 保留世界书的前后位置、深度、顺序、角色与 Outlet

**问题：** 主版已经复用酒馆原生扫描来决定“哪些世界书条目本轮被激活”，但激活之后仍偏向把内容汇总发送，尚未完整保留条目的布局元数据。因此“角色定义之前/之后”“示例对话之前/之后”“作者注释之前/之后”、`@Depth` 等条目即使被正确激活，也可能插到错误位置；同深度条目的 Order、Role 也可能失真。

**主版准备吸收：**

- 继续让酒馆原生扫描负责蓝灯、绿灯、递归、预算与链式激活，不重写关键词引擎。
- 扫描结果不能只返回字符串，还要保留位置、depth、order、role、outlet 等非敏感布局字段。
- 组装请求时支持角色定义前后、示例对话前后、作者注释前后和指定聊天深度。
- 相同深度按酒馆当前规则稳定排序；角色必须落到对应 SYSTEM／USER／ASSISTANT 消息，而不是统一改成 SYSTEM。
- Outlet 条目只在请求中出现对应 Outlet 宏时展开，不能因为“已激活”就自动发送。
- 长梦默认世界线隔离时完全绕过原世界书；只有用户明确沿用的冻结快照才能进入长梦布局，而且仍受 `canon` 最高优先级约束。

**完成标准：** 构造覆盖所有前后位置、两个相同深度不同 Order、不同 Role 和一个 Outlet 的世界书；诊断中显示的最终消息位置必须与酒馆正文请求的规则一致，同时蓝灯/绿灯现有测试继续通过。

#### 9.3 正确继承 Strict 语义，同时固定关闭工具调用

**问题：** 某些预设依赖酒馆 Chat Completion 的 Strict 处理或相关采样参数；插件自行拼接和直发时可能没有完整带上。另一方面，小剧场只需要生成正文/HTML，不需要工具调用。如果沿用当前连接的 tools、tool choice 或历史工具消息，可能占用上下文、触发不兼容，甚至让模型不输出正文。

**主版准备吸收：**

- 选择酒馆预设时，读取并保留与该预设对应的 Strict 处理和适用于本次请求的采样参数；先核对当前 SillyTavern 的真实字段和转换函数，再实现兼容层。
- 无论酒馆正文是否启用工具，小剧场请求都显式关闭工具能力，并清理只对工具链有效的字段和消息。
- OpenAI、Anthropic、千帆等协议分别采用其合法的“无工具”表达；不能为了关闭工具而发送目标后端不认识的字段。
- 不新增一个含义模糊的“Strict 开关”让用户猜；原则上跟随所选预设，并在诊断里说明本轮最终状态。

**完成标准：** 严格预设与普通预设各跑一组；请求中保留应有的预设消息/参数，但不含工具定义、工具选择或孤立工具消息。模型仍能正常流式输出正文，主 API和独立 API 都通过。

#### 9.4 把 API 连接隔离做成可证明的安全不变量

**问题性质：** 这不是已经确认“当前主版会偷用正文 Key”的现有 bug，而是 local.8 的连接重构提醒了一个值得长期防回归的边界。插件同时支持酒馆主 API 和独立 API 后，任何请求级覆盖、异常恢复或连接复用都必须保证两条线路不会串用模型、地址或密钥。

**主版准备吸收：**

- 选择“酒馆主 API”时，只走当前酒馆主线路；选择某个独立 API/保存连接时，只使用该连接明确对应的协议、Base URL、模型和密钥引用。
- 独立请求不得回退到正文 Key 来“帮用户成功”，除非界面明确选择并提示改走主 API；同样，主 API 不得误取某个独立预设的 Key。
- 若以后支持读取酒馆保存连接，覆盖只能作用于本次小剧场请求，不修改酒馆当前正文连接；成功、报错、停止和超时都必须恢复原状态。
- 保留插件现有的本地 API 预设能力，不强迫所有用户迁移到 TT `modelTargets` 或 ST Connection Profiles。
- 长梦记录、历史、诊断、日志和 ZIP 只保存连接/预设名称等非敏感引用，永远不保存、复制或展示 Key。

**完成标准：** 用两个带不同假模型名和假地址的测试连接并行构造请求，断言路由不交叉；模拟请求抛错和用户停止后，酒馆正文连接仍保持原值。日志只显示脱敏后的线路与模型信息。

#### 9.5 增加“实际发送给 AI 的内容”诊断视图

**问题：** 只有普通运行日志时，很难判断问题究竟来自预设排序、世界书激活、插入位置、宏展开、上下文裁剪还是 API 路由。用户看到“AI 串人设”时，如果不能查看最终消息，只能不断猜提示词。

**主版准备吸收：**

- 在用户主动展开诊断时，显示本轮世界书扫描候选与最终激活结果，以及每条激活内容的目标位置、depth、order、role、outlet。
- 显示宏、预设布局和裁剪完成后、真正交给请求层的最终消息数组；按消息角色分组，并清楚区分小剧场指令、聊天上下文、世界书和预设内容。
- 显示本轮使用主 API 还是独立 API、协议、模型、预设名、Strict 状态、工具是否关闭和各部分 Token 估算。
- 诊断默认不持久化；复制时继续使用现有脱敏管线，隐藏 Key、Authorization、Cookie、完整敏感 URL 和连接内部标识。
- 长内容允许折叠或仅显示摘要，但用户应能主动展开经过脱敏的完整消息；不能用程序“预计会发送”的假预览替代请求层实际收到的内容。

**完成标准：** 为同一请求在组装前后插入唯一标记，诊断最终消息与请求层输入逐项一致；复制诊断后自动测试确认不存在 Key、Token、Authorization 或敏感 URL 路径。

#### 9.6 五项改进的建议施工顺序

这条路线与长梦开发相关，但应作为独立的“请求还原度”工作流逐个落地，避免一次同时改世界书、预设、API 和生成控制器后无法定位回归。

1. **先做只读诊断骨架与测试夹具。** 在不改变现有请求的前提下记录当前最终消息、世界书结果和 API 路由，建立改造前基线。
2. **先锁定 API 隔离和无工具边界。** 这是请求传输层的安全不变量，范围较小，也能避免后续测试串线路。
3. **升级世界书扫描结果的数据结构和位置布局。** 先解决已经激活的条目“应该放哪里”，保持现有激活算法不动。
4. **再接原生预设的多消息布局。** 把世界书分区挂到正确锚点，并处理 `prompt_order`、角色和空标记。
5. **最后接 Strict/采样兼容并完善实际请求诊断。** 用诊断逐项对照酒馆正文消息，完成主 API、独立 API、不同协议和简单/复杂预设回归。

与长梦的衔接：当前可以继续完成控制器、请求适配器和界面接线，但在把“续写下一章”作为可发布功能真正开放给用户前，至少要审计 API 隔离、固定禁用工具，并接入可查看最终消息的基础诊断；长梦若选择继承世界书，再复用已经验证的布局模块。长梦默认隔离世界书时，不因为主版支持深度布局就自动读取原世界书。

#### 9.7 这次仍不纳入主版的 local.8 范围

- 只读取 AI 楼层、把最新 AI 正文固定改成虚拟 USER 的整套上下文分支。
- 当前楼层 MVU 分流、阶段性人设激活和 MVU 模板发送。
- 思考/变量标签过滤、swipe 停稳十秒触发、单楼一次等自动触发体系。
- 强制改用酒馆保存连接并删除插件自身 API 预设。
- 整体改走 `ChatCompletionService` 后端发送链，除非逐项验证确实是实现上述五项所必需。
- local.8 的完整 UI、历史阅读方式和其他个人化改动。

这些内容以后若出现独立、可复现且直接影响小剧场的需求，可以单项再评估；当前不能因为已经有二改代码就默认进入主版。

## 近期提交脉络

从新到旧：

- `3629fb5`：长梦第二轮流程与来源提示，并加入独立生成控制器基础
- `daa165b`：加入长梦入口、数据层、上下文组装器与预览骨架
- `8d97011`：发布 v3.6.3，修复全屏 HTML、历史 ZIP 导入和续写自动回到指令框
- `db5cda4`：对齐用户指令顺序，统一续写字数显示与前情截取口径
- `f620a33`：修复全屏阅读被插件窗口遮挡
- `29133f5`：加入暗色夜读与全屏阅读
- `1f6354a`：续写流程保留 HTML 结果，且续写请求不携带旧 HTML
- `e597753`：空流恢复与 429/繁忙处理
- `96c9407`：发布 v3.5.0，增强最终渲染与日志复制
- `bfb83db`：调整分段生成阈值
- `29954e4`：加入面向目标字数的长篇生成计划
- `129c341`：固定魔法棒菜单入口位置
- `f03c02e`：复用酒馆原生世界书激活逻辑
- `52fd173`：修复酒馆主 API，并压缩移动端 UI
- `b6c5f5c`：发布 v3.4.0

## 当前验收重点

每次动到生成、字数、续写或渲染逻辑后，至少覆盖这些场景：

1. 同一角色卡、同一条 `8000 字`指令连续跑 3 次，记录每次实际可读字数和请求轮数；三次均 `>= 6000 字`才算通过。
2. `3000 字`：通常应能单轮完成，确认模型首轮收到目标字数，且 HTML 丰富度正常。
3. `5000 / 6000 / 7000 字`：确认先得到完整纯正文，再单独排版；6000 字不能退化成普通简陋 HTML。
4. `8000 字`：确认正文按上下篇承接，最后统一排版；下半篇不能复述上半篇。
5. 构造“收束轮正常完成但不足 90%”场景，确认结局后不再发请求。
6. 构造第二轮 `Stream empty` 或 429，确认第一轮正文仍被保留，提示不归因于某类站点。
7. 手动连续续写两次，检查请求中只有上一轮纯正文，没有 A+B 全文，也没有 HTML/CSS/脚本；最终结果仍为所选 HTML 样式。
8. 世界书分别测试蓝灯常开、绿灯关键词命中/未命中，以及手机窄屏布局。
9. “复制日志”后粘贴检查内容确实是最新日志，且没有 Key、Token、Authorization 等敏感内容。

## 文件地图

- `index.js`：插件入口、界面、设置、生成总流程
- `long-dream.js`：长梦数据模型、规范化、定梦更新与最新章节读取
- `long-dream-payload.js`：长梦专用纯请求组装、冻结世界书隔离与上下文预算；测试分支已通过控制器接入实际 API
- `long-dream-generation.js`：阶段 3 独立生成控制器、停止信号、流式草稿检查点、排版后待确认状态与正式章节晋升；测试分支已接入 `index.js`
- `long-dream-backup.js`：长梦 JSON 备份的格式/版本校验与白名单导入导出规范化
- `long-dream-canon-suggestions.js`：阶段 4 只读首章的 AI 定梦建议请求、JSON/不确定项解析，以及只合并用户明确采纳项的 canon 纯逻辑
- `length-policy.js`：目标字数解析、分档、首轮篇幅引导
- `generation-job.js`：轮次、90% 完成线、动态收束与停止条件
- `generation-payload.js`：首轮、续写和最终 HTML 请求内容
- `text-counter.js`：可读字数统计、续写上下文窗口
- `api-client.js`：API 请求构造、协议兼容、响应字段与错误类型解析等纯逻辑
- `api-runtime.js`：酒馆主 API/独立 API 调用、SSE 与非流式读取、停止、429 重试、空流恢复和主 API 路径降级
- `request-diagnostics.js`：请求短错误信号、诊断问题汇总和安全错误分类
- `result-bookmark.js`：生成结果页边书签的位置归一化、可见范围计算与左右吸附
- `auto-mode.js`：自动模式指令来源解析与空来源状态
- `runtime-log.js`：内存日志和脱敏
- `safe-renderer.js`：安全 iframe 渲染与尺寸/正文上报
- `world-book-scan.js`、`world-book-policy.js`：酒馆世界书扫描接入与条目策略
- `style.css`：插件样式与移动端适配
- `settings.html`：设置面板模板
- `tests/core.test.js`：核心回归测试

## 修改与发布规则

- 修改前先运行 `git status --short --branch`，保留禾禾已有或无关改动。
- JavaScript 改动至少运行：

```powershell
node --check .\index.js
node --check .\api-client.js
node --check .\api-runtime.js
node --check .\safe-renderer.js
node --check .\request-metrics.js
node --check .\persona-follow.js
node --check .\notify.js
node --test --test-isolation=none .\tests\core.test.js
```

- 发布新版本时同步修改 `index.js` 的 `VERSION`、`manifest.json` 的 `version`、`style.css` 文件头版本，并确认设置页底部使用 `VERSION` 显示最新版。
- 禾禾没有明确说“push / 推送”时，不主动提交或推送。
- 推送后报告：版本号、分支、提交哈希、测试结果、工作区是否干净。
- 不提交 API Key、Token、Authorization、密码或用户日志中的敏感内容。

## 下一窗口的工作边界

- 阶段 4“AI 定梦建议，但保留用户主权”已完成代码与本地回归：建议只读首章，四类项目可修改、删除、采纳/撤回，不确定项明确标注；只有最终开卷时的已采纳项进入 `canon`，手写流程与 schema 保持不变。下一步是分别用酒馆主 API / 独立 API 做真实酒馆人工验收。
- 当前稳定版本仍为 `v3.6.3`。测试分支 `codex/long-dream-preview` 以 `617ca18` 为本轮基线；其后的当前 HEAD 加入长梦状态保留、待确认全屏与三版候选、主 API 兼容、世界线关系、梦脉织录、API 设置重排、手机适配和角色卡世界书跟随修复。
- 当前工作树包含阶段 3 收尾、HTML 误降级修正、阶段 4 与本交接的未提交改动；另有原有未跟踪目录 `standalone-scripts/`，始终保持未动。不要为了整理状态而覆盖、回退或提交它。
- `long-dream-payload.js`、`long-dream-generation.js` 与 `long-dream-memory.js` 已从 `index.js` 接入现有 API、最终排版和独立副 API 路径；主/独立 API 与角色卡世界书替换已经真实通过。长梦已具备章节目录独立阅读、完卷/恢复和 JSON 备份。
- 阶段 3 已接入正文自动补写、三版候选、连续五章防重复、停止草稿与排版恢复；正文完成后的排版失败只重试排版。真实酒馆仍需验证模型自然收束、真实流式多轮追加、第二轮停止恢复、排版时停止恢复及最终 HTML 只排版一次。
- 禾禾已批准把 local.8 暴露出的请求还原问题纳入主版路线，详见第 9 节。阶段 1 已实现 API 线路隔离回归、固定无工具、实际请求诊断、预设多消息布局与世界书八位置；剩余阶段继续做梦脉有效状态/检索、章节分支管理和大体量备份/发布前验证。
- 当前相关 JavaScript 语法检查通过，`node --test --test-isolation=none .\tests\core.test.js` 为 `137/137` 通过；阶段 4 新增验证只读首章、不确定项解析、未采纳项不进入 canon 和 UI 操作入口，同时保留此前的连续五章、防重复追加、排版恢复、iframe、请求保真、长梦三版、自动补写、`<thinking>` 隐藏和世界书回归。
- 当前没有版本升级；本轮实际 API 接线只进入独立测试分支，不改正式 `main`。
- 新反馈若涉及“字数不够”，先让禾禾提供复制后的脱敏运行日志，区分模型自然收尾、轮次策略、空流/429、最终排版失败，不要直接再次堆叠提示词。
- 新反馈若涉及“最终变纯文字/普通 HTML”，优先检查 staged render、最终占位校验和续写后的结果恢复，不要用关闭美化作为长期修复。
- 不要重新推翻已经定稿的 16384、90% 完成线、5000/8000 两个分段阈值和“续写只带上一轮纯正文”原则，除非禾禾明确决定更改产品方向。
