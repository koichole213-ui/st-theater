# 小剧场生成器 / Theater Generator

独立生成小剧场/番外内容的 SillyTavern 扩展插件。读取当前聊天的剧情上下文、角色设定、世界书和用户人设，在独立面板中生成小剧场，不影响正文剧情。

## 功能

- **独立生成** — 小剧场在插件面板内独立生成，完全不影响正文
- **自由指令** — 自由输入指令 + 保存常用指令为模板
- **渲染模板** — 内置默认模板，支持自定义（可粘贴各位老师的小剧场规则）
- **交互模式** — 可生成带点击/交互功能的小剧场
- **世界书选择器** — 下拉选择不同世界书、按条目展开/收纳/开关（v1.4）
- **User 人设** — 从酒馆读取或手动输入
- **流式传输** — 独立API自动启用SSE流式传输，实时显示，减少502/529超时（v1.4）
- **智能重试** — 主API遇到502/529自动重试（v1.4）
- **独立 API** — 可选择跟随主 API 或配置独立 API（支持OpenAI兼容/Anthropic格式）
- **历史记录** — 保存生成结果，随时回看
- **自定义 CSS** — 可自定义插件面板样式
- **数据导入导出** — 支持导出和导入所有数据

## 安装

### 方法一：扩展管理器（推荐）

Extensions → Install Extension → 粘贴仓库地址

### 方法二：手动安装

将文件放入 `data/default-user/extensions/third-party/st-theater/`

## v1.4 更新

- 世界书支持下拉选择不同世界书，条目可展开/收纳查看内容
- 独立API启用SSE流式传输，生成时实时显示文本
- 自动检测Anthropic/OpenAI格式API
- 主API添加502/529自动重试机制
- 世界书条目显示来源标签

## 兼容性

- SillyTavern 1.12.0+
- 支持所有 API

## 开源协议

AGPLv3
