# 千夜浮梦「梦脉 v2」冻结规格

> 状态：施工冻结稿（2026-08-08）
> 基线：`codex/long-dream-preview` / `6190a6b` / 插件 `v3.6.3`
> 目的：统一梦脉的产品语义、数据职责、增量协议、用户主权、迁移和二创边界。本文优先于早期讨论示例，但不替代用户验收。

## 1. 产品定义

梦脉是长梦的连续性档案：副 API 提议本批章节造成的增量变化，插件按白名单确定性归档，用户拥有最终校正和否决权，主 API 只读取整理后的资料续写。

梦脉不是章节摘要、世界书、用户确认的定梦，也不是主 API 临时总结出的设定。权威顺序为：本章明确要求（不得无提示推翻定梦）→ 用户确认的定梦 → 已保存章节 → 用户校正梦脉 → 自动梦脉 → 允许使用的冻结世界书。

## 2. 四层结构

### `states` 当前状态

- 保存下一章开始时仍成立的位置、伤势、关系、知情、身份、持有、完好状态、行动和目标。
- 槽位由程序管理；同一槽位只有一个当前值。
- 当前值改变时，旧值及其来源进入该槽位的 `history`；新值来源必须从新章节重新开始。
- 更新已有状态优先引用 `targetId`，新建时才根据 `subjects + attribute + topic` 生成槽位。

### `transitions` 关键变化

- 保存为什么会变成现在这样：人物弧光、关系转折、身份变化、长期经历和局势变化。
- 默认只追加；不因当前状态再次改变而删除。
- 普通动作和日常互动不得记录。

### `threads` 未完事项

- 统一管理伏笔、约定、谜团、秘密、任务和威胁。
- 生命周期为 `open → progressed → resolved` 或 `open/progressed → abandoned`。
- 顶层 `progress` 是最新概览，`progressHistory` 保存每次真正有价值的推进，不覆盖早期线索。
- 更新已有事项必须优先引用 `targetId`；只有建立新事项时使用自由文本 `threadKey`。
- 已解决、已放弃事项不常驻注入，但在本章方向明确命中时可以作为历史因果参与检索。

### `deviations` 世界线偏离

- 只用于明确继承原设定的平行支线、前传、同期补完和正史后续。
- 完全隔离模式拒绝此类操作。
- 分开保存原线事实、本梦改变、直接后果和此后失效的默认前提。
- 更新已有偏离优先引用 `targetId`。

## 3. `currentState` 定位

`currentState` 保留为副 API 生成的只读阅读摘要，不是独立权威事实，也不承担用户另写设定的职责。

- 用户发现错误时，应校正对应的状态、事项、偏离或定梦。
- 存在待确认冲突时，本批新摘要不得静默替换旧摘要。
- 主 API 请求必须明确：结构化梦脉和章节高于当前脉象。
- 旧版人工修改过的当前脉象在迁移时保留为兼容资料，不能无声丢弃。

## 4. 副 API 增量协议

固定返回：

```json
{
  "currentState": "截至本批最后一章的只读当前脉象",
  "operations": []
}
```

允许的操作只有：

- `set_state`
- `append_transition`
- `open_thread`
- `advance_thread`
- `resolve_thread`
- `abandon_thread`
- `upsert_deviation`

已有记录的更新通过 `targetId` 定位；新记录由程序生成 ID。模型不得输出 `delete`、`reject`、`hide`、`unlock` 或覆盖用户锁定内容的操作。

同一批多章的操作必须按 `chapterNumber`、再按响应原顺序稳定应用，确保连续变化形成正确历史。

## 5. 用户主权和冲突

下列语义必须分开：

- 隐藏：事实仍成立，但 `hiddenFromPrompt = true`。
- 校正并锁定：`editedByUser = true`、`lockedByUser = true`，AI 不得静默覆盖。
- 否定错误：建立分类型 `rejections` 抑制记录；近义复现进入待确认，不直接生效。
- 状态被取代：旧值进入 `history`，不视为错误或删除。
- 事项解决/放弃：保存结局和来源，不删除历史。

以下情况写入可持久化、可备份的 `pendingConflicts`：

- 操作命中用户锁定记忆。
- 命中用户否定抑制。
- 试图重新打开已解决或已放弃的同一事项。
- 更新引用不存在或类型不符的 `targetId`。

## 6. 主 API 注入

主 API 始终另外获得本章要求、定梦、最近四章全文和安全预算允许的旧章索引。梦脉使用分层软配额：

- 当前状态：建议 12。
- 开放/推进事项：建议 8。
- 世界线偏离：建议 5。
- 关键变化：建议 5。

空余名额可以流转；最终同时受总字符/Token 安全预算约束。当前脉象完整加入。已解决事项、旧状态历史和兼容旧卡片只在相关方向命中时低优先级加入。

## 7. v1 迁移

- 使用 `memory.schemaVersion` 区分版本。
- 原始 v1 卡片必须保留；无法可靠结构化的内容进入 `legacyCards`。
- 状态、关系、事件、伏笔等只在可以保守判断时迁移；不得为了结构整齐猜测主体、属性或生命周期。
- 迁移失败时仍能以纯文本方式注入旧记忆。
- 回退、删章和分支第一版继续采用“清空自动梦脉、保留定梦、按保留章节重新织录”，不实现逐操作逆向回滚。

## 8. 提示词预设 JSON

v2 第一版只开放分析侧重点，不开放自定义字段、操作、合并模式或状态机：

```json
{
  "format": "st-theater-long-dream-memory-preset",
  "version": 2,
  "name": "人物弧光与因果梦脉",
  "author": "",
  "description": "",
  "focusPrompt": "",
  "outputContract": "long-dream-memory-v2",
  "tags": [],
  "homepage": "",
  "license": ""
}
```

程序固定提供底线规则和 JSON 输出合同；预设作者只能改变重要性判断、分析侧重和表达偏好。导入文件不得携带代码、HTML、密钥、API 配置、长梦正文或用户梦脉。同名导入创建副本，不静默覆盖。

自定义结构包等四层结构经过多部长篇验证后再讨论。

## 9. 第一版验收底线

- 当前状态不重复建槽，旧值历史和新值来源正确。
- 重要人物/关系变化不会因状态覆盖而丢失。
- 未完事项可以建立、推进、解决和放弃，推进过程可追溯。
- 用户锁定和否定内容不会被 AI 静默覆盖或复活。
- 完全隔离模式没有世界线偏离。
- 冲突在刷新和备份后仍存在。
- v1 任何无法迁移的内容都不无声丢失。
- 梦脉失败不阻止章节保存和续写。
- 主 API 不承担梦脉总结或合并工作。
